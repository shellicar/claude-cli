# 22:30 — Documentation mission, Courier (Phase 3, shipped)

This PR replaces claude-cli's stale, legacy-app user-facing docs with an accurate set in the SC's voice, on branch `docs/documentation`. The Writer phase was committed by the SC (`7f10d4b`); this phase added the changelog entries, pushed, and opened the PR. No release fires — the SC runs releases separately.

## What the PR contains

- **Seven READMEs.** Root `README.md` is the canonical full doc; the other six are lean and link home. New: `apps/claude-sdk-cli`, `packages/mcp-exec` (the one real standalone README — it's a usable MCP server), and minimal stubs for `claude-sdk-tools`, `claude-core`, `exec-core`. Rewritten: root and `packages/claude-sdk` (replaced its `> TODO`).
- **Deletions:** `docs/architecture.md`, `docs/roadmap.md`, `docs/sdk-findings.md`, `docs/skills-design.md` (all described the removed legacy `apps/claude-cli`), plus the stale `schema/cli-config.schema.json`. The live schema is the generated `sdk-config.schema.json`.
- **Config default change (the SC's own, folded into the Writer commit):** `schema.ts` now defaults to `claude-opus-4-8` / `thinking.effort: high`, with its test and regenerated `sdk-config.schema.json`. The root README's example config matches.
- **Changelog (this phase):** an entry in all six published packages' `changes.jsonl` (`apps/claude-sdk-cli`, `packages/claude-sdk`, `claude-sdk-tools`, `claude-core`, `mcp-exec`, `exec-core`), with all `CHANGELOG.md` regenerated via `pnpm --filter scripts run changelog`. `changes.jsonl` and `CHANGELOG.md` must never drift — regenerate in the same change or CI fails.

## Facts the docs commit to — don't "helpfully" undo them

- **Config file is `sdk-config.json`**, not the legacy `cli-config.json` (which survives only in dead references).
- **Code execution is not wired.** The docs list web search + web fetch as the only server tools. Code execution has a config surface but is never added to the request (`buildServerTools` never pushes it). Do not add it back to the docs.
- **Thinking display:** live thinking always streams to the transcript; only the *replay* visibility is configurable (`historyReplay.showThinking`). The docs say "watch it live, read it back on replay" and deliberately do not call the live display configurable.
- **CLAUDE.md loads into messages**, not the system prompt. **SYSTEM.md** is the system-prompt variant — that distinction is the only reason SYSTEM.md earns its sentence.
- **`claude-sdk-cli.log` lands in the launch directory, not under `.claude`** (`logger.ts` passes a bare filename). The "Files and storage" section documents only what the CLI writes automatically.
- **Build:** turbo at root, tsup for libs, esbuild for the CLI app only. The old README's "esbuild" monorepo line was wrong.

## Decisions

- **Root README title is `@shellicar/claude-sdk-cli`** (the installable package the npm badge points at), not the repo name `@shellicar/claude-cli`. The Writer resolved the Scout's "title is wrong" flag this way.
- **Link style differs by surface on purpose:** package READMEs ship to npm and use absolute `github.com` URLs (relative links break there); the root is the GitHub front page and uses relative paths.
- **`exec-core` is in the changelog.** The first dispatch's list omitted it; the Courier flagged the inconsistency (its stub README was created this PR, the same as `claude-core` / `claude-sdk-tools` which were included), and the Handler's mission update confirmed the omission was a mistake — so it got an `added` entry and a `pkg: exec-core` label in a follow-up commit.

## Traps

- **The dated testament file collides across PRs.** `.claude/testament/YYYY-MM-DD.md` is shared, and each Courier rewrites it for their own PR. While this PR ran, the `feature/core-di-rework` PR rewrote `2026-06-28.md` as *its* testament; merging `origin/main` interleaved the two into a tangle. Keep your PR's legacy in your own dated file, and after merging main, check `git diff origin/main -- .claude/testament` shows only your file.
- The `Exec` tool's `steps` schema rejects on this host (Scout and Writer both hit it). Use `ExecV2`, or `bash -lc` via `Exec` with a single command, for shell work.

# Memory tool — the consolidated record

The persistent, shared, relevance-searchable Memory tool: built in Phase 2, then rebased onto the
`core-di-lite` #378 refactor. This is the single durable record — it folds in the Phase 2 build notes
and supersedes the earlier "DI constraint" section (written pre-#378, now wrong). What the code and
the git log can't tell you lives here.

## What it is, and how it's built

- **`packages/claude-core/src/memory/`** — the contract: `IMemoryStore`, the types, the pure
  `toFtsMatch` search helper, and `IMemoryEnvironmentProvider`. Timestamps are ISO strings on the
  contract; `@js-joda` stays confined to the stores, never the library runtime.
- **`SqliteMemoryEngine`** (app, `persistence/`) — the real store, a plain class: constructor
  `(db, clock)`, all the SQL, DDL in the constructor. One **ordinary** FTS5 table (not contentless,
  not external-content, so `UPDATE`/`DELETE` work for the soft-delete) with `title/body/keywords`
  indexed and `id/type/keywords_json/environment/created_at` UNINDEXED. `keywords` is stored twice on
  purpose: indexed text (ranks) plus `keywords_json` (returns the array faithfully). bm25 weights
  `10.0, 1.0, 4.0` give title > keywords > body; column order fixes the weight positions. This is the
  class the integration tests run over `:memory:`.
- **`SqliteMemoryStore`** (app, `persistence/`) — the injectable `IMemoryStore` adapter:
  `@dependsOn(SqliteMemoryEngine)` + `@dependsOn(IMemoryEnvironmentProvider)`, resolves environment
  per write, delegates the rest. (No lazy open — the engine is built and injected by the container.)
- **`GitMemoryEnvironmentProvider`** — resolves `org`/`project`/`repo` from the live git remote and
  `fleet` from config, **on every write**, so a mid-session remote/cwd change is reflected.
- **`packages/claude-sdk-tools/src/Memory/`** — the five tools (`WriteMemory`, `ReadMemory`,
  `SearchMemory`, `DeleteMemory`, `MemoryTypes`). Thin handlers that map and shape; no store logic.
- **Rendering** consumes the existing client `tool_result` SDK event (on main from history-view #364)
  via `AgentMessageHandler`; `ToolObject.setResultLine` carries SearchMemory's count + top title.

## The load-bearing safety piece

`toFtsMatch` (pure, claude-core): match `/[\p{L}\p{N}]+/gu`, wrap each token in double quotes, join
with ` OR `. OR (not AND) because the goal is relevance and recall, not exact conjunction. No-token
input returns `null`, the store returns `[]`, never errors. Every FTS5 operator a user might type
(`hello -world`, quoted phrases, `OR`/`AND`/`NEAR`, `star*`, `(paren)`, bare `-`) is reduced to
quoted literals, so none survives to reach the engine. A bareword `OR` becomes the search term
`"OR"`, which is the point. The full adversarial input-safety set runs green against the real engine.

## DI: when `@dependsOn`, when a factory — the rule (the SC's)

`core-di-lite` is property injection, used everywhere on main (grep `@dependsOn`): the container does
`new Impl()`, *then* sets the `@dependsOn(Token) private readonly x!: T` fields, and registration is a
bare `.to(Impl)`. Because fields are set after construction, a constructor cannot read them.

- **Configuration and wiring live in `container.ts`.** That is the file's whole purpose: create,
  resolve, configure. A class never sources its own dependency or config — no singleton import, no
  config read, no `new`-ing a dependency in a constructor. If you see that, it belongs in the container.
- **`@dependsOn` + bare `.to(Impl)`** when every dependency is a resolvable token. This is the default.
- **A factory closure `.to(Token, (x) => new Impl(...))` only when a constructor argument is not a
  token** — a runtime/derived value: a db handle, a cwd-derived string, a config slice, an assembled
  array/map. That, and only that, earns a factory.
- **Eager init in a constructor is fine and good** — opening a db, running DDL/migrations — *as long
  as it acts on what was handed in*; it hides nothing. This is why a factory-built class can do DB work
  in its constructor and an `@dependsOn` class cannot: the latter's fields aren't set yet.

So Memory wires this way: the store and the environment provider are `@dependsOn` classes (bare
`.to(Impl)`); `SqliteMemoryEngine` is the **single** factory, because its db is not a token and the
db-file name (`config.memory.tenantId` → `memory.db` / `memory.<id>.db`) is configuration, computed
in `container.ts` and handed in. Same shape as `SqliteObjectStore`. Don't "simplify" the store to a
plain constructor — `@dependsOn` is what matches everything around it.

## The test-double lesson (the most important one on this mission)

A test double with behaviour means your unit test tests the double, not the unit. An earlier
`FakeMemoryStore` was a working substring store, and the handler "unit tests" asserted its filtering —
testing the fake. The fix: `RecordingMemoryStore` is a **spy** that records the call argument and
returns a canned value, nothing else. Handler tests assert only **mapping** (the captured arg proves
`intent` is dropped and the fields forwarded) and **shaping** (canned return → output shape). Every
search/filter/rank/count assertion lives in the integration tier against the real engine. One ranking
implementation, so never a parity test. The double's form (stub/spy/fake) is the operator's call.

## Asserting on log output is an anti-pattern of the worst kind

`migrate.spec.ts` / `SqliteMemoryEngine.spec.ts` had once been written to inject an `ILogger` and
assert on `logger.entries` — testing the narration instead of the behaviour. The `RecordingLogger`
they imported was never committed beside them; it lives in `stash@{6}`, but that stash is a *separate,
broad ILogger-injection refactor* — never paired with migrate, so don't resurrect it here. Resolution:
no injected logger (migrate is `(db, migrations, name)`, engine is `(db, clock)`); the tests that read
logs now assert behaviour. If a test says "it logged X," it is testing the wrong thing; assert what the
system *did*. (`migrate.ts` itself still logs through an imported `logger` singleton — the
hidden-dependency reach the DI rule would lift into the container. Left as-is, flagged.)

## SC design decisions the code doesn't explain

- **`intent`, not `description`.** The per-call narration field (cross-tool convention, as `Exec`
  carries) where Claude narrates the call to the SC for visibility: required input, **not stored, not
  searched, not ranked**. Present on write/read/search/delete, absent on `MemoryTypes`.
- **Environment resolves per write, never at startup.** The git remote can change mid-session, so a
  launch-time snapshot would stamp a stale origin. The provider resolves live at write time.
- **Schema migrations** (`migrate.ts`) are an append-only list, versioned in `PRAGMA user_version`
  (`major*1000 + minor`; minor = additive/tolerated, major = destructive/refused). The version is
  re-checked *inside* the `BEGIN IMMEDIATE` write lock so two CLIs racing the same migration can't
  double-apply — the whole reason the shared `~/.claude` store needs the lock. See CLAUDE.md
  "Database Schema & Migrations".

## Traps that cost real time

- **biome runs only in `pnpm run ci`, never in `type-check` or `test`.** It will fail CI on
  import-sort, formatting, barrel files, and member-accessibility that a green gate sailed past — it
  flagged 18 errors after a clean type-check/test once. Hand-fix the non-auto-fixable ones (barrel
  file, `public` modifiers), then `pnpm ci:fix` (never `biome check --write` directly). Run before pushing.
- **`createMemoryTools` returns a tuple, not an array.** `return [Write, Read, …]` infers as
  `Array<unionOfAllFive>`, so tests calling a specific tool fail type-check. Cast to the explicit tuple
  of `typeof` each tool.
- **No `export *` barrel.** Biome's `noBarrelFile`/`noReExportAll` reject it at error level; the entry
  file uses explicit named re-exports, matching the existing `Exec.ts` shape.
- **FTS5 + bm25 + porter confirmed on this machine's Node** via the engine integration tests (the
  `CREATE VIRTUAL TABLE … USING fts5` in the constructor is the earliest-signal check, and it passed in
  a real run). The bundled SEA's Node is the one remaining binding risk; construct-on-`:memory:` re-checks it.
- **To take main's side of a conflicted file:** `git show origin/main:path > path`. `git checkout
  --ours` and `git restore` are blocked by safe-operations.
- **The dated testament files are `merge=union` and shared across concurrent missions.** A rebase can
  make your copy *look* like it replaced another mission's entries; the union driver recombines both
  sides at merge. Leave others' entries alone; before shipping run
  `git diff origin/main...HEAD -- .claude/testament` to confirm you only added your own. (This is why
  `2026-06-27.md` also carries an unrelated repaint-every-cell entry — it's main's, folded in by union.)

## Foreign file in the worktree

`.claude/CLAUDE.md` is untracked fleet/harness scaffolding the SC stashes and pops; it predates this
work and is not part of the feature. Do not sweep it into a commit.
# 00:35

## Composable toolset redesign — Apostle phase 1, iteration 3 (plan written)

Plan lives in the Handler repo: `~/repos/fleet/claude-fleet-shellicar--cli-paths/projects/claude-cli/missions/2026-06-27_composable-toolset-redesign/plan.md`. Read it with `discovery.md` (the why) and `scenarios.md` (the settled design reference) colocated.

### What iteration 3 is, vs 1 and 2
The 2026-06-29 mission update + `scenarios.md` make the tool set and names **SC-decided**, not the Apostle's to propose: `Find, Paths, Read, Match, Head, Tail, Range, Slice, Pipe`. Iteration 1 flagged names as open and flagged two claude-core gaps as blockers (scope said "touch nothing standard"). Iteration 2 collapsed Head/Tail/Range into one `Slice`. Both are now wrong: scope was relaxed (touching standard/claude-core allowed), and all four windowers ship deliberately (usage data on which the model reaches for). My plan renders the decided names and all four windowers — no names re-opened, no proposals where decisions exist.

### Grounding that shaped the design (read the real code, not just the docs)
- **Stream discriminator must be `kind`, not `type`** — `FileRecord` has its own `type` field (`file|dir|link`), which collides with the current transport's `type: 'files'|'content'` discriminator (`pipe.ts`). Used `kind` for the stream union.
- **The graft is one line** in `Pipe/Pipe.ts`: `const toolInput = pipeValue !== undefined ? { ...step.input, content: pipeValue } : step.input;`. The rewrite removes it by threading a typed `Stream` object that never enters `step.input`.
- **Three faces fit a new `ComposableTool` type**, not the existing `ToolDefinition` (`claude-sdk/src/public/types.ts`) — that one is single-faced (`input_schema` is both model and handler input), which is exactly what the graft exploits. New `composable.ts` carries `model` (pure Zod), `pipe: {in,out}` (string tokens), `reconcile` (the sole bridge), `run` (canonical only). Sources adapt to the old `ToolDefinition` via a `toStandalone` helper for top-level registration; stages are pipe-only (audit: transducers ~never standalone).
- **Polymorphic edge** — Match/Head/Tail/Range/Slice are type-preserving over either stream. Proposed an `'any'`/`'same'` edge token so one tool/one schema covers both, rather than registering each twice. Flagged as a Maker shape decision (§8.7), not a wall.
- **`Grep` already dispatches on `content.type`** (`Grep/Grep.ts`) — the `Match` polymorphism precedent is real.
- **`SearchFiles` formats `path:line:text`** (`SearchFiles.ts`) precisely because `PipeContent` has only a block-level path. Grouped `Content` (path on the file) removes the dodge — that's the whole reason for the new shape.

### The two claude-core reaches (now in-scope Maker work, §8.1/8.2)
- `walk` (`claude-core/src/fs/walk.ts`) returns `string[]`, discards the type it computes per entry. `Files` needs `{path,type,size}` records → `IFileSystem.find` must return records. `find-symlinks.spec.ts` pins the string behaviour and moves with it.
- `IFileSystem` has no `readlink`; `realpath` resolves the whole chain (wrong for the one-hop `name -> target` flatten). Add `readlink` to interface, `NodeFileSystem`, and `MemoryFileSystem` (whose `readdir` hardcodes `isSymbolicLink: () => false` — symlinks aren't representable in the fake yet).

### Two judgment calls in the plan worth knowing
1. **grep -l paths-only is NOT built.** `scenarios.md` shows a `Content → path` projection for "which files contain it." The locked decision fixes projection to the two flatten forms keyed on terminus kind. So `Find | Read | Match` flattens to grouped Content (path headers + matching lines — which already shows which files matched); the terse paths-only output is the *customisable select* the mission defers. I rendered the fixed flatten and did not invent a select mechanism. Flagged §8.5 rather than build past the decision.
2. **`Read` drops the binary branch.** `Read` derives from `ReadFile`'s read path but binary-in-a-pipe is fatal (locked error table + SC's no-binary-in-pipe ruling). The Maker must drop `detectBlock`'s binary branch, not carry it. §8.6.

### Broken reference (same as iteration 1/2 reported)
Mission's `## Agents` points to `fleet/agents/operators/apostle.md` — does not exist; the dir is `agents/handler/` only, plus a `templates/prompt-authoring/blocks/apostle.md` whose content matches the Apostle role already in the mission's Phase 1. Lost nothing. Not a skill/script so not a hard critical failure, but a standing broken path the Handler should fix or the mission should drop.

### State
Preflight clean (SC cleared the prior testament + updated with main before the cast). Worktree, `feature/composable-toolset-redesign`. Read-only in this repo — only wrote the plan (Handler repo) and this testament. No code staged (Apostle plants no stakes).


# 04:20

## Same cast — plan rewritten after the SC caught it deferring

My first plan (00:35 entry) deferred design decisions to the Maker dressed as "hard shapes": "the Maker can either change walk's return type or add walkRecords," "the Maker should pick the 'any'/'same' token," "a decision on whose size to report." The SC rejected it hard and correctly — the Apostle's deliverable IS the code I would write into the files, and "the Maker compares to this plan as a diff" only works if the code is actually in the plan. Prose pointing at a decision is a hole with a label on it.

Rewrote the whole plan with every shape rendered as code and every choice made:
- **`walk` → `FileRecord[]`** rendered in full: builds records, swallows a mid-traversal `readdir` failure at the recursive call site (the locked swallowed error), top-level failure left to throw (fatal). Takes the one-hop symlink `target` from `readlink`, dir target gets trailing `/`.
- **`readlink`** added across `IFileSystem`, `NodeFileSystem`, and the `MemoryFileSystem` fakes — and the fake gains a `links` map (path → target) + `setSymlink`, because today it hardcodes `isSymbolicLink: () => false` and symlinks aren't representable, so the flatten couldn't be tested otherwise. Rendered.
- **`FileRecord` moves to claude-core** (`fs/records.ts`) because `walk` (claude-core) must return it; claude-sdk-tools' `stream.ts` builds the Zod schema on the same fields. Type from the dir entry, size from `stat`. Rendered the decision and the sourcing.
- **Polymorphic edge** resolved to `pipe: { in: 'any', out: 'same' }` with concrete `edgeAccepts`/`resolveOut` helpers in the Pipe.
- **Every tool handler** (`Find`, `Paths`, `Read`, `Match`, `Head`, `Tail`, `Range`, `Slice`) rendered as a full `defineComposable` with its `run` body, not a `// run:` comment. Shared `windowGrain` helper for the four windowers; `Read` sniffs binary with `fileTypeFromBuffer` and throws `PipeStepError`.
- **`PipeStepError` → Pipe run-loop catch → `{ step, tool, error, input }`** rendered; `toStandalone` adapter rendered.
- **§9** lists every decision made; the one genuinely-SC item (the terse grep-l paths-only output, a locked deferral) is the only thing not built, and reopening it is named as the SC's call, not assumed.

Lesson for any later Apostle cast in this repo: do not write "the Maker decides X" or "X can be done either way." Decide it and render the code. If a decision is truly the SC's, it is a locked/deferred item named as such — not a punt to the next operator.


# 21:18

## Same cast — mission re-issued (mission-updated) for the run-time-fatal gap

Handler sharpened two requirements in the mission and recast me. The gap: the error model promises run-time fatals (a thrown handler — missing `Paths` path, unreadable/binary `Read`) map to `{ step, tool, error, input }`, but the plan needed that shown *in code* in the `Pipe` AND a test pinning the run-time path (not just the pre-flight one).

My 04:20 rewrite already had the run-time mapping in §4's run loop (the `try/catch` returning `fatal(...)`). What was missing against the new bar: §4 prose didn't *name* the two-phase split, and §7's throw-tests asserted at the **handler** level (`createPaths(fs).run(...)` rejects), not the **Pipe** level (the pipe returns the JSON, resolves not rejects). Two targeted edits:
- §4 prose split into "Pre-flight fatals" (first loop) and "Run-time fatals" (second loop's `catch` arm), naming that the raw throw never escapes the pipe.
- §7 added three run-time-fatal tests that run *through* `call(pipe, ...)` with a non-existent `Paths` path and assert the returned object's `tool`/`step`/`input` — `call` resolving rather than rejecting is itself the "not thrown" proof. One assertion each, expected/actual style.

Note for the record: the supervisor PASS and the Handler's "weighed" note both describe the *first* plan (the `'any'`/`'same'` deferral, §4 pre-flight-only) — they were written against 00:35, before my 04:20 concrete rewrite. So the run-time mapping was already present when the recast arrived; this pass only made it explicit (prose) and pinned it (tests), which is what the updated mission asks for in code.


# 23:11

## Composable toolset redesign — Phase 2, Maker (iteration 1), build complete

Built the full redesign from `plan.md`. All three gates green: `pnpm run type-check`, `pnpm run test` (460 tools tests + the rest of the workspace), `pnpm run build` (incl. the app's `tsx build.ts`).

### What landed
- **claude-core**: new `fs/records.ts` (`FileRecord`/`FileType`); `walk.ts` rewritten to return `FileRecord[]` (swallows a mid-traversal `readdir` failure at the recursive call site, top-level throw left to surface as fatal, one-hop symlink `target` via `readlink`); `IFileSystem.find` now returns `FileRecord[]` and `readlink` is an abstract method.
- **claude-sdk-tools**: new `stream.ts` (Files/Content typed streams + `flattenFiles`/`flattenContent`/`formatSize`), `composable.ts` (the three-face `ComposableTool`, `defineComposable`, `reconcile`, `PipeStepError`, `toStandalone`), `window.ts` (`windowGrain`). New tools `Paths`, `Read`, `Match`, `Slice`; `Find`/`Head`/`Tail`/`Range` rewritten onto the streams; `Pipe` rewritten (pre-flight type-check + run-time `try/catch` mapping every throw to `{step,tool,error,input}`). Deleted `Grep/`, `SearchFiles/`. `NodeFileSystem.readlink` added. New entries + package exports for Paths/Read/Match/Slice/composable; Grep/SearchFiles exports removed.
- **app**: `createAppTools` wires sources (`Find`,`Paths`) standalone via `toStandalone`, stages pipe-only, `ReadFile` into the standard set.

### Deviations from the plan (compiler-led or codebase-fact corrections)
1. **No per-tool `reconcile` field.** The plan hung `reconcile: reconcileDefault` on every tool. TS cannot infer `TIn` from a `StreamFor<TIn>` parameter (can't invert a conditional type), so the generic `reconcileDefault` would not assign by-reference to each tool's field — every tool errored. Dropped the field; `reconcile(model, upstream)` is one function in `composable.ts`, applied once by the Pipe and `toStandalone`. This *is* the plan's "sole bridge" intent, more literally (one function, not one-per-tool). `Canonical`'s `run`-param typing is unchanged, so handlers still narrow (`Read` sees `FilesStream`, `Match`/windowers see the union and discriminate).
2. **`pipe.ts` kept, trimmed to `PipeFilesSchema`.** The plan said delete it. `DeleteFile`/`DeleteDirectory` import `PipeFilesSchema` as their direct model input (`{content:{type:'files',values}}`), and Delete is out of scope (parked terminal). Removed only the superseded transport schemas (`PipeContent`/`PipeInput`/`RegexSearchOptions`); kept `PipeFilesSchema`.
3. **MemoryFileSystem symlink support not added.** Plan §8.2 wanted a links-map on the tools-test `MemoryFileSystem`. Kept the dedicated `SymlinkMockFileSystem` in `find-symlinks.spec.ts` (added `readlink`) — lower risk, same coverage. The two `MemoryFileSystem` fakes (tools + app) and the claude-core test fake got a throwing `readlink` (correct: `walk` only calls `readlink` in its symlink branch, which file-only fakes never enter).
4. **`toStandalone` uses `as unknown as AnyToolDefinition`.** Genuine type-erasure boundary — `ToolHandler<never>` contravariance makes neither face assignable to the other (the codebase's own `AnyToolDefinition` documents this). Anchored with a comment.
5. **`ReadFile` got its own local content schema** instead of `PipeContentSchema` (same shape: `{type:'content',values,totalLines,path}`).
6. **`runStage` test helper** added to `helpers.ts` — `Read`'s empty model makes its canonical `Record<string,never> & {input}`, which rejects a plain-object argument; the helper passes the canonical loosely (`as never`), mirroring how the Pipe builds it.

### Gotcha for the next cast
`TsDiagnostics` served **stale** results across rapid edits in this session (kept citing a removed `reconcile` field, byte-identical messages after the line changed). Don't trust it mid-refactor — the real `tsc`/`tsup` build is authoritative. Every "error" it reported on the foundation turned out to compile clean.

### State
Branch `feature/composable-toolset-redesign`. Staged only my own files (explicit paths). Not committed (Maker phase). Pre-existing staged testaments from the prior cast were left as found.
