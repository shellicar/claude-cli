# 02:24 — Phase 2 Writer (documentation mission), iteration 2

The documentation set is written, deletions done, staged, not committed (Phase 2 is stage-only; the Courier commits). Read `scout.md` in the Handler mission dir first — its inventory is accurate and I verified the load-bearing facts against the code before writing.

## What shipped

Seven READMEs (5 new, 2 rewritten) plus 5 deletions:
- Root `README.md` — rewritten as the canonical full doc.
- `apps/claude-sdk-cli/README.md` (new) — small npm README, links home.
- `packages/mcp-exec/README.md` (new) — full standalone MCP-server README.
- `packages/claude-sdk/README.md` (rewritten) — minimal stub, replaced the `> TODO`.
- `packages/claude-sdk-tools`, `claude-core`, `exec-core` READMEs (new) — minimal stubs.
- Deleted: `docs/architecture.md`, `docs/roadmap.md`, `docs/sdk-findings.md`, `docs/skills-design.md`, `schema/cli-config.schema.json`. (`docs/` is now empty; git ignores empty dirs, harmless.)

## Decisions a reader of the code won't see

- **Root title is `@shellicar/claude-sdk-cli`, not the repo name `@shellicar/claude-cli`.** That is the installable package and what the npm badge points at. The scout flagged the old `@shellicar/claude-cli` title/badge as wrong; I resolved it to the real CLI package.
- **Link style differs by surface, on purpose.** Package READMEs ship to npm (`files` includes `README.md`), where relative links break, so they use absolute `github.com/shellicar/claude-cli` URLs. The root README is the GitHub repo front page (root package is `private`), so it uses relative paths — matching the old root README.
- **Ecosystem markers:** kept the empty `<!-- BEGIN_ECOSYSTEM -->`/`<!-- END_ECOSYSTEM -->` pair in the root (preserved as instructed; tooling fills them). Did NOT add them to the other six — adding empty markers that may never be filled is cruft.
- **Voice:** removed "by design" from the auth note (sc-ghostwriting flags it as a slogan tell; the plain reason "for personal use with your own account" already carries it). Kept "on purpose" (the SC's own phrasing, reads as plain speech).

## Facts I re-verified against code (don't re-chase)

- **Thinking display:** live thinking always streams into the transcript — `main.ts:294` wires `thinking_text` → `message_thinking` unconditionally, rendered as a 💭 block. Only the *replay* visibility is configurable (`historyReplay.showThinking`, default false; `replayHistory.ts`). So I wrote "watch it live, read it back on replay" and did NOT call the live display configurable.
- **Server tools:** root doc lists web search + web fetch only, both default-on. Code execution is deliberately absent (scout §10: config surface exists, never added to the request). Do not let anyone "helpfully" add it back.
- **Config file is `sdk-config.json`** (not the legacy `cli-config.json`). Keys and defaults documented from `cli-config/schema.ts` directly.
- **Build:** turbo at root, tsup for libs, esbuild for the CLI app only. The old README's "esbuild" monorepo line was wrong; fixed.

## For the Courier (Phase 3)

- Changelog: the mission names 5 packages for `changes.jsonl` entries (`claude-sdk-cli`, `claude-sdk`, `claude-sdk-tools`, `claude-core`, `mcp-exec`). **`exec-core` also got a new README but is NOT in that list** — confirm with the SC/Handler whether it wants an entry too, or leave it per the brief. Use `changed` for root + `claude-sdk` (rewrites), `added` for the rest.
- Legacy-reference purge is complete in the user-facing docs: the root README and `claude-sdk` README were the only two carrying legacy `claude-cli`/Agent-SDK references, and both were rewritten. The scout flagged root/app `CLAUDE.md` and `CHANGELOG.md` as out of scope (agent/repo files) — left untouched.

## Mechanics note

The `Exec` tool's `steps` schema still rejects on this host (the scout hit the same). Use `ExecV2` for shell commands.


# 02:50 — Phase 2 Writer, follow-up (Files and storage)

The Handler added a "Files and storage" item to the root README spec. Added a section documenting every path the CLI writes, confirmed against code. Three things diverged from the brief's framing:

- **The log is in the cwd, not `.claude`.** `logger.ts:49` gives the winston File transport a bare `filename: 'claude-sdk-cli.log'`, so it lands in the directory you launch from, not under `.claude/`. Documented as "outside `.claude`".
- **`persistence.db` exists and the brief didn't list it.** `container.ts:116` — `~/.claude/persistence.db`, a SQLite store for Ref + PreviewEdit state across restarts. Included it.
- **Audit is a directory, global.** `~/.claude/audit/<id>.jsonl`, one file per session (`container.ts:126` + `AuditWriter.ts:14`). Global, not per-directory.

Global (`~/.claude`): sdk-config.json, .credentials.json, conversations/<id>.jsonl, audit/<id>.jsonl, persistence.db. Per-directory (`./.claude`): sdk-config.json, .sdk-conversation-id, .sdk-conversation-history. Cwd: claude-sdk-cli.log.

## Co-working change I did NOT make — needs the SC's call

After I staged in the first iteration, another session edited the root README's example config: `"model": "claude-sonnet-4-6"` → `"claude-opus-4-8"` and `"effort": "max"` → `"high"`. The verified default (`schema.ts`, `DEFAULT_MODEL`) is `claude-sonnet-4-6`/`max`, and I cannot confirm `claude-opus-4-8` is a real model. I left their edit in place (overwriting an uncommitted change would destroy it) and surfaced it for the SC to decide: keep opus-4-8/high, or restore the verified sonnet-4-6/max. The staged README currently carries their values, flagged.


# 04:13 — Phase 2 Writer, follow-up (Files and storage, reads vs writes)

Two corrections to the Files and storage section, from the SC via the Handler:

- **Dropped the "main reason ~/.claude grows" line.** It was editorial interpretation, not a fact (it traced to a Handler instruction to explain why .claude fills up). The section now states what is written and where, no reason-for-growth claim.
- **Split reads from writes**, confirmed against code. The earlier version conflated them (it listed sdk-config.json as a write, which it isn't except via --init-config).
  - **Reads** (user authors, CLI reads): `CLAUDE.md` (`ClaudeMdLoader`), `SYSTEM.md` (`SystemPromptLoader`), `sdk-config.json` (`consts.ts` + watcher at `main.ts:176`). `--init-config` (`initConfig.ts`) writes a starter `~/.claude/sdk-config.json` only if none exists (`existsSync` guard); the local `./.claude/sdk-config.json` is never written by the CLI.
  - **Writes** (CLI produces): `.credentials.json` (login), `conversations/<id>.jsonl` + `audit/<id>.jsonl` + `persistence.db` (global), the two cwd markers `.sdk-conversation-id` / `.sdk-conversation-history` (`#writeMarker` / `#appendToHistory` in `ConversationSession.ts`), and `claude-sdk-cli.log` (launch dir).

The opus-4-8 / high example config is confirmed correct: the SC changed `schema.ts` (`DEFAULT_MODEL = 'claude-opus-4-8'`, effort default `high`) and `cli-config.spec.ts` after the first read. Those code files plus the regenerated `schema/sdk-config.schema.json` are his, left unstaged (out of the docs scope).


# 19:16 — Phase 2 Writer, follow-up (mission-file corrections)

The mission file gained two Phase-2 corrections; applied them to the root README:

- **Terminal Setup, one block per platform.** Windows Terminal had two json blocks (the `sendInput` action, then the keybinding); combined them into a single `settings.json` snippet with `actions` and `keybindings` arrays, parallel to the VS Code and macOS single blocks. Escape strings left exactly as they were (`\\u001b[13;5u` in the file, which is the JSON string the terminal then interprets — correct, not a double-escape bug).
- **Gotchas: dropped the intro sentence** ("A few sharp edges worth knowing.") so the heading and bullets stand alone; **rewrote the effort bullet** to call the `thinking.effort` nesting a mistake plainly, not explain what effort does (that is Anthropic's), and link Anthropic's effort docs. Also trimmed the matching "governs all token spend" explanation from the Configuration cross-ref line for consistency (a small extension of the brief's effort directive, surfaced here and in the debrief).

The SC staged his three code files (`schema.ts`, `cli-config.spec.ts`, `sdk-config.schema.json`) himself between turns; left them staged as he set them. The Files-and-storage section was unchanged this iteration (it already matched the updated brief and passed supervisor iteration 2).


# 19:28 — Phase 2 Writer, follow-up (pretty-printed JSON)

The mission gained an editorial principle: every JSON example is standard pretty JSON, the same throughout (each property on its own line, objects and arrays expanded, no compact single-line objects). Reformatted every JSON code block to 2-space, fully expanded:

- Root README: the Configuration example (`thinking`, `tools`, `webSearch`, `webFetch` were compact); the three Terminal Setup blocks (also normalised from 4-space to 2-space so all blocks match).
- mcp-exec README: the exec-tool example (the command object and the `args` array were inline). The MCP client-config block was already pretty.

Left the inline `--config '{"model":"..."}'` example in the flag table compact: it is a single-line shell argument (how you actually pass it), not a document JSON example, and a markdown table cell cannot hold a fenced block. Flagged in the debrief, not silently decided.


# 19:56 — Phase 2 Writer, follow-up (distinctive-only Features, tighter prose)

Four mission corrections, all to the root README:

- **Features: distinctive only.** Dropped the parity bullets that Claude Code does natively (CLAUDE.md, web search/fetch, binary reads, OAuth) — listing them was the sales-inventory habit. Kept the distinctive set: composable tools + `Pipe` (reframed: chain tools without routing every result back through Claude), structured `Exec` instead of Bash, TypeScript LSP, visible thinking (live + after the fact), flat JSONL conversations (dropped the coined label "Scriptable sessions"), command mode, simple permissions. Seven bullets.
- **Gotchas effort: a phrase, not a build-up.** Now "`thinking.effort` is a misnomer: effort is not specific to thinking" + the Anthropic docs link. No explanation of what effort does.
- **Auth: two sentences.** No API-key flow / uses your own Claude account like Claude Code; an API key goes in `~/.claude/.credentials.json`. Dropped the OAuth login-flow paragraph (covered in Installation).
- **Files and storage: automatic writes only.** Removed the "Files it reads" subsection (CLAUDE.md, SYSTEM.md, sdk-config.json) and the reads/writes framing entirely; the section is now just the files the CLI writes on its own, global vs per-directory vs launch dir.

Removed-from-Features facts still live where they belong: web search/fetch and CLAUDE.md in the Configuration keys, OAuth in Installation, JSONL detail in the Sessions section. The SC's three code files remain staged as he set them.


# 20:35 — Phase 2 Writer, follow-up (Tools + Context files sections)

Two new sections added to the root README, placed together in the reference cluster (after Terminal Setup, before Permissions):

- **Tools** — a plain grouped reference of the tool names, one line per group, from the Scout's inventory (verified against code): read and search (`Find`, `Grep`, `SearchFiles`, `ReadFile` for text/PDF/image, the `Head`/`Tail`/`Range` slices, chained with `Pipe`), edit (`PreviewEdit`/`EditFile`, `CreateFile`, `AppendFile`, `DeleteFile`, `DeleteDirectory`), `Exec`/`ExecV2`, `Ref`, the four `Ts*` tools, and the server tools (web search, web fetch). Included `Grep` though the brief's parenthetical omitted it — it is a real registered tool in scout §4, and the section's purpose is "what Claude actually has."
- **Context files** — one line of CLAUDE.md support (same locations as Claude Code, no mechanics or locations enumerated, per the brief) plus the SYSTEM.md distinction: same mechanism, but into the system prompt instead of the conversation.

Did not add a Claude Code CLAUDE.md docs link — the brief said link only "if there is a clean one," and I have no verified URL, so I stated support without one. Everything else in the doc is from prior iterations (supervisor PASS through iteration 3). The SC's three code files remain staged as he set them.
