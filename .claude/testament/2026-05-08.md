# 18:50

## Phase 2 — Scaffolder: wrapped arrow nav (#319)

### What I wrote

All test-file changes from the Apostle plan and the SC's verification block landed in `apps/claude-sdk-cli/test/EditorState.spec.ts`:

- Five rewrites that swap `key('up')` cursor-positioning setup for direct constructor calls (`delete`, `ctrl+delete`, `ctrl+k`, both `right` cross-line tests).
- Removal of the old `describe('EditorState — up')` and `describe('EditorState — down')` blocks plus the `// up / down with col clamping` section header.
- Two new `describe` blocks at the bottom: `EditorState — moveUpVisual` (8 tests, including the SC's wide-character addendum) and `EditorState — moveDownVisual` (8 tests, including the SC's clamp-overshoot addendum).

### Test state after this phase

`pnpm test` from the workspace root: 8 of 9 tasks pass; `@shellicar/claude-sdk-cli` reports 20 failures of 501. Breakdown of the 20:

- 4 of the 5 rewrites fail (`abcd`/`firstsecond` joins and the `right — at EOL wraps to next line index` cursorLine assertion).
- 16 new tests fail with `TypeError: s.moveUpVisual/moveDownVisual is not a function`.

The 5th rewrite (`right — at EOL wraps to start of next line`) passes accidentally. JavaScript silently ignores the unsupported constructor argument, so `new EditorState({ lines: ['ab', ''], cursorLine: 0, cursorCol: 2 })` lands at the default state `{ lines: [''], cursorLine: 0, cursorCol: 0 }`. Then `right` from `cursorCol=0` on an empty line stays at 0, which is what the test expects. Once Phase 3 adds the constructor, the test exercises the real path and still passes — no fix needed.

### Asking before writing

Phase 2 as originally written demanded `pnpm type-check` to pass while the new tests reference APIs (constructor argument + two methods) the Builder has not added. The plan also keeps all those API additions under section 2 (Builder territory). I stopped and asked the SC; they revised the prompt to drop type-check from the verify step, expand the Tests section to cover all test-file changes from the plan, and explicitly state that the new and rewritten tests will fail until Phase 3.

### Working-tree note

`.claude/testament/2026-05-04.md` was already staged (left over from the previous Apostle run) when I started. Preflight surfaced it; I left it staged per the established convention from this branch.

### For the Builder

The plan's section 2 is your contract; nothing in the test file needs to move. The accidentally-passing test (`right — at EOL wraps to start of next line`) will still pass after the constructor lands. No other gaps surfaced.

# 19:08

## Phase 3 — Builder: wrapped arrow nav (#319)

### What I applied

The non-test diffs from the Apostle plan, all to `apps/claude-sdk-cli/src/`:

- `model/EditorState.ts`: added `stringWidth` import, `colFromVisual` helper, `EditorStateInit` named type, optional-state constructor, removed `case 'up'` / `case 'down'` from `handleKey`, added `moveUpVisual` / `moveDownVisual` methods. Updated the `lines` getter doc to drop the obsolete "Read-only" claim.
- `view/renderEditor.ts`: exported `EDITOR_PREFIX_VISUAL_WIDTH = 3`.
- `AppLayout.ts`: added `EDITOR_PREFIX_VISUAL_WIDTH` to the `renderEditor` import; intercept `'up'` / `'down'` after the command-mode guard, before forwarding to `editorState.handleKey`.

After the implementation: `pnpm build`, `pnpm type-check`, `pnpm test` all green from the workspace root.

### Lint deviations from the plan

The plan's exact code tripped three biome rules:

- `if (targetVisual <= 0) return 0;` and `if (w + sw > targetVisual) return index;` violated `lint/style/useBlockStatements`.
- `constructor(initial?: EditorStateInit) {` violated `lint/style/useConsistentMemberAccessibility` (no `public`).

`pnpm ci:fix` skipped these as unsafe. I applied them by hand: wrapped the two `if` bodies in braces and added `public` to the constructor. The original plan said "Code style is the Cleaner's job, not yours; do not refactor surrounding code" — but these are errors in the code I just added, not surrounding code, and the project's CI gate (`pnpm run ci`) blocks on them. The Courier would hit a red CI on push otherwise. The fixes don't touch behaviour.

After the fixes, `pnpm run ci` is clean alongside the other checks.

### Changelog

Added one entry to `apps/claude-sdk-cli/changes.jsonl`:

```jsonl
{"description":"Up/down arrows now move between visual rows when input wraps, instead of skipping over the wrapped portion","category":"fixed"}
```

`pnpm exec tsx src/validate-changes.ts` from `scripts/` reports no errors.

### Branch state for the Courier

`fix/wrapped-arrow-nav` is 3 commits behind `origin/main`. Two of those commits added entries to `apps/claude-sdk-cli/changes.jsonl` (security update, dependency bump). The diff against `main` shows my new line as added and those two as missing — that's the merge that the Courier phase handles. My new entry will append cleanly after both because it sits at the end of the file.

### What's staged after this phase

Implementation + changelog + AppLayout interception. Test file was already staged by Phase 2. Testament also staged.

# 20:10

## Phase 4 — Courier: wrapped arrow nav (#319)

### What shipped

Up/down arrows in `claude-sdk-cli` now move the caret between visual rows of wrapped input, instead of skipping over the wrapped portion to the next logical line. The fix is additive — the buffer/caret model still stores logical lines, the renderer still wraps for display, and `EditorState` is unchanged in shape. Two new methods do the visual-row arithmetic, and `AppLayout` calls them before `EditorState.handleKey` ever sees the keypress.

### How the pieces fit

- `EditorState.moveUpVisual(cols, prefixWidth)` and `moveDownVisual(cols, prefixWidth)` compute the current visual position via `prefixWidth + stringWidth(line.slice(0, cursorCol))`, derive the row and the column-in-row, and find the offset in the destination row whose visual width matches. The destination clamps to a grapheme boundary if the same column would land mid-character.
- `colFromVisual` is a module-level helper that walks graphemes via the existing `graphemeSegmenter`. The overshoot branch (`if (w + sw > targetVisual) return index`) is what gives wide characters their clamp behaviour.
- `EDITOR_PREFIX_VISUAL_WIDTH = 3` is exported from `renderEditor.ts`. `AppLayout` imports it and passes `this.#screen.columns` for `cols`. Single edit point if the prompt prefix ever changes width.
- `AppLayout.handleKey` intercepts `'up'` and `'down'` between the command-mode guard and the `editorState.handleKey` call, then `scheduleRender()` and returns.

### The trap that shaped this

`EditorState.handleKey` originally had `case 'up'` and `case 'down'` that moved by logical-line index. They became unreachable once `AppLayout` started intercepting, but a chunk of unrelated tests (`delete`, `ctrl+delete`, `ctrl+k`, both `right` cross-line tests) used `key('up')` purely as a cursor-positioning step before exercising something else. That backchannel kept dead branches alive for test convenience.

The fix: add an optional `EditorStateInit` argument to the constructor so tests can declare their starting state directly (`new EditorState({ lines, cursorLine, cursorCol })`), then remove the dead branches and rewrite the dependent tests to construct state instead of typing it. The constructor defensive-copies the `lines` array — without that, an external array mutation would propagate into editor state. `EditorStateInit` is a named module-scope type, not an inline literal, to match the codebase style.

The old `describe('EditorState — up')` and `describe('EditorState — down')` blocks went with the dead branches. The new contract lives under `describe('EditorState — moveUpVisual')` and `describe('EditorState — moveDownVisual')`.

### Coverage gap recorded for follow-up

Tests are ASCII-only except for two wide-char cases the SC added during Phase 1 review. `cursorCol` is a code-unit offset and `stringWidth(line.slice(0, cursorCol))` is a visual width; for ASCII they collapse to the same number, so a future edit that swaps one for the other could pass every test. The two wide-char tests (`'中'.repeat(7)` and `'a' + '中'.repeat(6)`) exercise the `colFromVisual` overshoot branch through the public methods. Not enough to fully close the gap; recorded so whoever picks it up can see the shape.

### Style fixes the Builder applied

`pnpm ci:fix` left three biome violations untouched as unsafe. The Builder fixed them by hand because they were errors *in the new code*, not surrounding code: braces around two single-line `if` returns in `colFromVisual` (`useBlockStatements`), and `public` on the constructor (`useConsistentMemberAccessibility`). CI gate (`pnpm run ci`) blocks on these.

### Merge state

Branch was 3 commits behind `origin/main` at Courier dispatch. Two of those commits added entries to `apps/claude-sdk-cli/changes.jsonl` (a security update and a dependency bump). My entry sits at the end of the file, so the merge appends cleanly with no conflict.

### Manual verification

SC ran the CLI on the worktree at the end of Phase 3 and confirmed wrapped up/down navigation works as specified. Recorded in the prompt's Delivery Notes block (2026-05-08 20:02), not in the Phase 3 Debrief block — the template question of whether `## Debrief` belongs in the prompt at all is still open.
