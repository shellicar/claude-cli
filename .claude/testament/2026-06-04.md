# 22:30

Security audit Phase 1 for 2026-05-31 mission. Three CVEs, all resolved.

## What changed

`fix-audit.sh` added three overrides to `pnpm-workspace.yaml`:
- `qs@>=6.11.1 <=6.15.1: '>=6.15.2'` — GHSA-q8mj-m7cp-5q26
- `turbo@<=2.9.13: '>=2.9.14'` — GHSA-hcf7-66rw-9f5r
- `turbo@>=1.1.0 <2.9.14: '>=2.9.14'` — GHSA-3qcw-2rhx-2726

ncu bumped patch/minor deps across all workspaces (excluding `@anthropic-ai/sdk`, typescript, diff, syncpack, pnpm — all skipped; see below).

## Decisions

`@anthropic-ai/sdk` skipped at supervisor's direction: pre-1.0 minor bumps are realistically breaking. Worth a dedicated update later.

`typescript` 5→6 skipped (major). `diff` 8→9 skipped (major, claude-sdk-tools). `pnpm` 10→11 skipped (major, packageManager field).

## CVE scope note

All three CVEs are workspace-root-level fixes. turbo is a root devDependency (not in any published package's package.json). The qs vulnerability is through `apps/claude-cli > @modelcontextprotocol/sdk > express > qs` — legacy app only, not published. Per skill rules, no security changes.jsonl entries in published packages; only the dep-update entry (`"Updated patch and minor dependencies"`, `"category":"changed"`).

## Peer dependency warnings

`apps/claude-cli` (legacy, unpublished) shows unmet peer warnings for `@shellicar/build-clean` and `@shellicar/build-version` (expect esbuild@^0.27, found 0.28.0; expect vite@^7, found 8.0.16). These are in the legacy app only. Not affecting published packages or build/test results.

## Phase 2 scope

All five published packages have package.json dep-bump changes:
- `apps/claude-sdk-cli`
- `packages/claude-core`
- `packages/claude-sdk`
- `packages/claude-sdk-tools`
- `packages/mcp-exec`

All five need version bumps in Phase 2.