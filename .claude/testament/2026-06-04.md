# 23:43

Security audit for 2026-05-31. Three CVEs resolved, five packages bumped to beta.7, PR shipped.

## CVE fixes

All three fixes live in `pnpm-workspace.yaml` overrides — none touch published package deps directly:

- `qs@>=6.11.1 <=6.15.1: '>=6.15.2'` — GHSA-q8mj-m7cp-5q26 (qs.stringify TypeError)
- `turbo@<=2.9.13: '>=2.9.14'` — GHSA-hcf7-66rw-9f5r (CSRF/session fixation)
- `turbo@>=1.1.0 <2.9.14: '>=2.9.14'` — GHSA-3qcw-2rhx-2726 (arbitrary code execution, CVSS 9.8)

The qs chain runs through `apps/claude-cli > @modelcontextprotocol/sdk > express > qs` (legacy, unpublished). turbo is a root devDependency. Neither reaches any published package's direct deps. This is why `changes.jsonl` entries are `category: changed` (dep update), not `category: security` — the CVE fixes are in root-level overrides, not in published package deps.

## ncu sweep exclusions

- `@anthropic-ai/sdk`: pre-1.0 minor bumps are realistically breaking; reserved for a dedicated update
- `typescript`: 5→6 is major
- `diff`: 8→9 is major (affects claude-sdk-tools)
- `pnpm`: 10→11 is major (packageManager field)
- `syncpack`: excluded by the maintenance-release-fleet skill

## apps/claude-cli peer dep warnings

Unmet peer warnings for `@shellicar/build-clean` and `@shellicar/build-version` (expect esbuild@^0.27, found 0.28.0; expect vite@^7, found 8.0.16). Legacy app only, not affecting build or published packages. Pre-existing, not introduced by this audit.

## Version bumps

All five published packages moved from `1.0.0-beta.6` to `1.0.0-beta.7`. No release markers in `changes.jsonl` — pre-release process keeps everything under `[Unreleased]`.

## claude-sdk-cli CHANGELOG

Regeneration pulled in the `--name`/`--model`/`--prompt`/`--no-resume` and wrapped-input caret entries from `changes.jsonl` entries that had accumulated since the last CHANGELOG generation. This is correct behaviour (generator caught up), but the audit PR's diff includes changelog lines unrelated to the audit. Not a problem, just surprising if you see it in the diff.
