# esbuild CVE maintenance release (2026-06-15)

Branch `security/audit-2026-06-15`, PR #352. Two esbuild advisories, both patched at `>=0.28.1`: GHSA-gv7w-rqvm-qjhr (high, RCE via `NPM_CONFIG_REGISTRY` in the Deno install path) and GHSA-g7r4-m6w7-qqqr (low, Windows dev-server path traversal). The Phase 0 audit found nothing else.

## The fix changes nothing downstream

esbuild is dev/build-only across the whole repo: a devDependency in the five packages, otherwise transitive through tsup, vite, build-clean, build-version. It never enters a published bundle, so the CVE is consumer-invisible. The fix is a root `pnpm-workspace.yaml` override (the repo's established CVE mechanism, same as vite, hono, qs, minimatch), which touches no published package's own `package.json`. So no `changes.jsonl`, no version bumps, no release. The SC chose override-only with no patch sweep; nothing else in the repo had a runtime patch waiting.

## Traps for the next operator

- **`fix-audit.sh` over-nukes the lockfile.** It applies the pnpm#6774 lockfile rebuild unconditionally, but #6774 only matters for *chained* overrides (one override feeding another). This CVE has no chaining (one package, two ranges, both resolving to `>=0.28.1`), so the nuke was not needed and floated unrelated dev tooling up within its `^` ranges (biome, turbo, knip, and friends). The SC redid it surgically: restore the lockfile, keep the override, plain `pnpm install`. The float vanished and the diffstat dropped from ~1122 to 598 lines. For a single-package, non-chained CVE, skip the nuke or gate it on real chaining.
- **pnpm 10 misreports where it writes the override.** `pnpm audit --fix` says "added to package.json" but actually writes to `pnpm-workspace.yaml`, where this repo keeps overrides. Verify the location, do not trust the message.
- **Testaments are tracked and live in the worktree.** claude-cli whitelists `.claude/*/**/*.md` in `.gitignore`, so testaments belong in the worktree on the branch and ship with the PR, not in the main checkout. The mission's full-path testament line was a scaffold bug (`substituteTestamentPath` forces the full-path form and preempts the `check-ignore` probe); flagged for a separate fleet fix.
- **`.claude/audit/<date>.json` is committed by convention.** Prior security PRs (#332, #328) shipped their audit captures, so the Phase 0 capture correctly rides into the fix commit.

## Ship

PR #352 carries the override, lockfile, audit capture, and this testament. Root-only change, so labels `dependencies` and `security` with no `pkg:` label. CI was clean bar checks still running at hand-off.

# 20:50 — beta.9 release prep (Phase 1 Maker)

Branch `feature/version-beta9`. Bumped the five publishable packages to `1.0.0-beta.9` (claude-core, claude-sdk, claude-sdk-tools, mcp-exec, claude-sdk-cli), regenerated changelogs, removed the legacy `apps/claude-cli`, dropped the dead `npm_token` env block from `npm-publish.yml`, and documented the transitive-bump rule in root `CLAUDE.md`. Verify suite (install/build/type-check/test/ci) all green. Staged, not committed — handed to the supervisor.

## Release-set confirmation

Source diffs since last tags: claude-core/claude-sdk/claude-sdk-tools changed since their `beta.7` tags; claude-sdk-cli changed since `beta.8`. `mcp-exec` had **zero** source changes since `beta.7` — it bumps purely transitively (depends on claude-sdk-tools, which bumps). Consequence: regenerating mcp-exec's changelog produced **no diff**, so only its `package.json` is staged, not its `CHANGELOG.md`. That is expected, not a miss. The other four changelogs changed. All five changelogs keep everything under `[Unreleased]` (pre-release → no release markers), confirmed.

## Traps for the next operator

- **DeleteFile aborts the whole batch on the first real directory (EISDIR).** Deleting the legacy app's leftover `node_modules` (pnpm symlinks), a `.vite-temp` real dir sat mid-list and killed the batch — earlier entries deleted, later ones didn't. Separate symlink leaves (DeleteFile) from real dirs (DeleteDirectory, deepest-first) into distinct calls.
- **`pnpm install` prunes the lockfile but leaves the physical worktree `node_modules`.** After deleting the tracked files of `apps/claude-cli` and running install, the lockfile dropped it but the on-disk `apps/claude-cli/node_modules` (symlinks) and emptied source dirs remained. "Delete the directory" needed a manual scrub of those leftovers with the Delete* tools.
- **knip.json still names `apps/claude-cli` — left deliberately.** knip runs in no verify gate (lefthook = biome only; CI = build/type-check/test/`biome ci`; knip is just a devDependency). knip.json already carried an unrelated stale entry (`packages/claude-cli-tools`, nonexistent), so it isn't kept in sync and doesn't gate the build. Out of the mission's build-green scope; flagged not fixed.
- **Root `CLAUDE.md` package table still has the `apps/claude-cli` row.** Not pruned — the mission scoped the CLAUDE.md edit to the Lockstep-versioning rule only. A future docs pass may want to drop that row and the `apps/claude-sdk-cli/CLAUDE.md` reference notes pointing at the removed app's source.

## Ship

Staged set is 90 files: 5 `package.json` bumps, 4 changelogs, 78 deletions under `apps/claude-cli`, `npm-publish.yml`, `CLAUDE.md`, `pnpm-lock.yaml`. Proposed commit message below in the debrief. Phase 2 (Courier) opens the PR.
