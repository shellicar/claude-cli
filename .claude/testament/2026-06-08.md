# 20:05

Scaffolder cast for configurable tool permissions (#101). Branch `feature/configurable-permissions`. No implementation landed — stubs and failing tests only.

## What was built

Two seams established:

**Seam 1 — `getPermission` signature change** (`permissions.ts`): added `_matrix: PermissionConfig = permissions` as the 4th parameter. The default keeps `AgentMessageHandler.ts` compiling without modification (existing 3-arg call site still works). The stub removes zone logic and always reads from `permissions['default'][operation]` — it compiles and handles the existing AgentMessageHandler test cases (read-inside→Approve, delete-inside→Ask, unknown→Deny) but fails for outside-zone and Pipe-with-outside-step cases. `ZonePermissions` and `PermissionConfig` are now exported.

**Seam 2 — `permissions` block in schema** (`cli-config/schema.ts`): added `permissionActionSchema` (enum: approve/ask/deny), `zonePermissionsSchema`, and `permissionsSchema` following the exact pattern of every other block (field-level `.catch()`, object-level `.optional().default().catch()`). Stub defaults are all `'deny'` so the tests fail clearly. The field is wired into `sdkConfigSchema`.

## Test contract (for the Builder)

6 new tests fail; 564 pre-existing tests pass:

**`permissions.spec.ts`** (3 failures, 5 passes):
- inside-cwd: read→Approve, write→Approve, delete→Ask — all PASS with stub (default zone happens to be correct)
- outside-cwd: read→Approve — PASSES (by coincidence: default.read = outside.read)
- outside-cwd: write→Ask — FAILS (stub returns Approve)
- outside-cwd: delete→Deny — FAILS (stub returns Ask)
- Pipe [read-inside, write-outside] → Ask — FAILS (stub returns Approve, ignores zone)
- unknown tool → Deny — PASSES

**`cli-config.spec.ts`** (3 failures):
- "returns defaults for empty object" snapshot — FAILS (stub has all-deny)
- "permissions defaults to the current permission matrix" — FAILS
- "permissions falls back to defaults on invalid value" — FAILS

## What the Builder must do

1. Implement `getPermission` to use the passed `matrix` for lookups. Restore zone logic using `getPathFromInput` + `isInsideCwd`. Remove the `_` prefix (matrix is now used). Remove the module-level `permissions` const once nothing reads it.

2. Fill in `permissionsSchema` with correct defaults matching the original `permissions` const:
   - `default`: read=approve, write=approve, delete=ask
   - `outside`: read=approve, write=ask, delete=deny

3. Wire the live matrix to the call site in `AgentMessageHandler.#toolApprovalRequest`. The handler is constructed once and reused; the live matrix should be obtained from `configLoader.config.permissions` at approval time (same pattern as `getEffectiveModel` reading `configLoader.config.model`). Pass only the matrix, not the whole config or ConfigLoader.

4. Map config strings (`'approve'`/`'ask'`/`'deny'`) to `PermissionAction` enum values at the call site or in a helper.

## Key facts for the Builder

- `getPermission` tests build the matrix inline with enum values — the tests don't test the config schema, they test the function. The Builder maps config strings to enum values before passing to `getPermission`.
- The `AgentMessageHandler` is constructed once in `main.ts` before the agent loop. It needs a live read of `configLoader.config.permissions` at approval time, not a snapshot from construction. The `getEffectiveModel` closure (line ~181 of main.ts) is the exact pattern: a function that reads `configLoader.config.X` at call time, passed to the handler.
- The `AgentMessageHandler` stage list includes `AgentMessageHandler.ts` AND `main.ts` — both need touching.
- The module-level `permissions` const can be removed once the default parameter is gone from `getPermission`'s signature.
- `getPathFromInput` and `isInsideCwd` remain in `permissions.ts` — they're not dead code, just temporarily unused by the stub. Don't remove them.

## Worktree note

Linked worktree (`claude-cli--configurable-permissions`). `pnpm install` was needed before build/type-check. Build via `pnpm build` at repo root (turbo handles dependency order).
