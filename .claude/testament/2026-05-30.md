# 16:15

Scaffolder for launch-flags-followup (Phase 2, iteration 2). Re-scaffold against
the Apostle's corrected plan. Type-check clean; 24 new tests failing for the
right reason.

## What changed from iteration 1

Iteration 1 was blocked for two patterns:
- Pattern 1: inline `expect(...).toBe(literal)` in four `statusBar` cli-config tests
- Pattern 2: seven tautological tests that pass at scaffold time

The Apostle's third revision dropped all seven tests:
- Four `statusBar` cli-config tests: dropped (schema defaults are declarations)
- `isModelOverridden starts false`: dropped (field initialiser)
- `showConversationId starts false`: dropped (field initialiser)
- `setModel(name) defaults isModelOverridden to false`: dropped (passes vacuously, adds no signal)

The iteration 2 scaffold was built from scratch against the corrected plan.

## What landed

**New source stubs:**
- `src/decodePromptEscapes.ts` — stub that throws; all 7 decode tests fail
- `src/view/parseModelName.ts` — `ParsedModelName` type + stub that throws; all 8 tests fail
- `src/model/StatusState.ts` — added `#modelOverridden`, `#showConversationId`, getters for
  both, `setModel(name, overridden = false)` with `overridden` param but not stored,
  `setShowConversationId` as no-op stub
- `src/model/ConversationSession.ts` — `#loadHistoryForId` and `resume` stubs that throw
- `src/cli-config/schema.ts` — `statusBarSchema` added (default `true`) and plumbed into
  `sdkConfigSchema`; cli-config defaults test passes because schema is the implementation
- `src/view/renderStatus.ts` — `renderModel` third parameter `conversationId: string` added;
  existing body unchanged; `abbreviateModel` stays
- `src/AppLayout.ts` — `renderModel(this.#statusState, cols, this.#session.id)` call updated

**New test files:**
- `test/parseModelName.spec.ts` (8 tests, all fail — stub throws)
- `test/decodePromptEscapes.spec.ts` (7 tests, all fail — stub throws)

**Modified test files:**
- `test/ConversationSession.spec.ts` — `resume` describe block (3 tests, all fail — stub throws)
- `test/StatusState.spec.ts` — `modelOverridden` and `showConversationId` blocks (2 tests: both
  fail — stub setters don't store the values)
- `test/renderStatus.spec.ts` — deleted old-style name test, added ANSI import, all existing
  `renderModel(state, 120)` calls updated to `renderModel(state, 120, '')`, four new describe
  blocks (6 new tests: 4 fail, 1 passes, 1 omitted per plan)
- `test/cli-config.spec.ts` — defaults `toEqual` assertion updated to include
  `statusBar: { showConversationId: true }`

## Failing test summary (24)

All 24 fail because the stub returns the wrong thing or throws:
- `parseModelName`: 8 (function throws)
- `decodePromptEscapes`: 7 (function throws)
- `ConversationSession — resume`: 3 (method throws)
- `StatusState — modelOverridden`: 1 (`setModel(name, true)` ignores the flag)
- `StatusState — showConversationId`: 1 (`setShowConversationId` is a no-op)
- `renderStatus — model name and version`: 1 (stub shows no version for sonnet-4-6)
- `renderStatus — override marker`: 1 (stub shows no `*`)
- `renderStatus — conversation id`: 2 (stub ignores `conversationId` and `showConversationId`)

## One test passes vacuously (expected, noted)

`renderStatus — model name and version — omits the version when the model has no trailing
numerics` passes at scaffold time: `abbreviateModel('claude-opus')` returns `'Opus'` and the
existing body produces ` ${YELLOW}⚡ Opus${RESET}  my-project` which matches the expected
string exactly. This was anticipated in the Apostle's plan and is acceptable: the test guards
the no-version shape and will continue passing once `parseModelName` is implemented.

## Line range error caught during editing

First attempt to delete the old-style test in `renderStatus.spec.ts` used `startLine: 140,
endLine: 147` instead of `startLine: 139, endLine: 146`. This left the old-style test's
`it(` header intact and replaced its body with the Opus test body. Caught from the diff
before EditFile. Second PreviewEdit with the correct range was clean.

## For the Builder

- `abbreviateModel` in `renderStatus.ts` is deleted and replaced with `parseModelName`
  (Item 6c's full replacement). The Builder replaces both `abbreviateModel` and `renderModel`
  with the plan's Item 6c block.
- `#loadHistoryForId` and `resume` in `ConversationSession.ts` currently throw. Builder fills
  them per Items 2b and 2c.
- `setModel` and `setShowConversationId` in `StatusState.ts` are stubs. Builder fills per
  Items 6a and 1b.
- The main.ts changes (Items 2a, 2b, 3, 4b, 5, 6b, 1e) are not scaffolded — pure
  implementation, no tests to pre-define for them.
