# 01:51

Courier for opus-4-8 pricing and context-window support.

## What shipped

`packages/claude-sdk/src/private/pricing.ts`:
- Added `claude-opus-4-8` to PRICING at opus-4-7 rates ($5/$25 per million input/output)
- Added `claude-opus-4-8: 1_000_000` to CONTEXT_WINDOW
- Corrected `claude-sonnet-4` in CONTEXT_WINDOW from 200k to 1M
- Added `claude-opus-4-5: 200_000` and `claude-opus-4-1: 200_000` explicit pins (see trap below)
- Updated `getContextWindow` fallback via `getFamilyDefault()`: `^claude-(opus|sonnet)-` → 1M, everything else → 200k

`packages/claude-sdk/test/pricing.spec.ts` (new):
- 10 tests covering `getContextWindow` behaviour
- Pinned models, family fallback for opus/sonnet/haiku, and precedence guards proving the map is checked before fallback

## The trap: opus-4-5 and opus-4-1 needed explicit 200k pins

The new `getFamilyDefault` (`^claude-(opus|sonnet)-` → 1M) captures any unrecognised opus or sonnet id. `claude-opus-4-5` and `claude-opus-4-1` are authoritatively 200k but were not pinned before this change -- they were hitting the old flat-200k fallback correctly. With the new family rule, they would have resolved to 1M.

The fix is two explicit pins. The SC made this call after the Maker was blocked on the first cast. The precedence-guard tests in `pricing.spec.ts` cover both.

If you add a future model to the family fallback, check whether it should be 200k -- the pattern is `claude-opus-4-X` where X is a specific version number, not a suffix family marker.

## Authoritative context window reference

From the Anthropic models endpoint at the time of this change:
- 1M: opus-4-8, opus-4-7, opus-4-6, sonnet-4-6, sonnet-4-5, sonnet-4
- 200k: opus-4-5, opus-4-1, opus-4, haiku-4-5

## Worktree bootstrap

This worktree ships without `node_modules`. Run `pnpm install` before any build or test step.

`pnpm exec tsx` from `scripts/` fails. Use `scripts/node_modules/.bin/tsx` from the main worktree at `~/repos/@shellicar/claude-cli/` instead.

## Branch state at handoff

Branch `feature/opus-4-8-support` has one commit beyond the branch point (`61d4fb6`). Since branching, PR #334 ("Retry transient API errors with exponential backoff") merged to main -- `origin/main` is now at `ceb37b9`. The changes are in entirely different files (backoff.ts, TurnRunner.ts) with no conflict with pricing.ts/pricing.spec.ts.

# 12:16

Courier — decide-dist-tags mission complete.

## What was built

Replaced `scripts/get-npm-tag.sh` (and the `scripts/tag-latest.sh` workaround) with a TypeScript decision layer that correctly handles both channel assignment and `latest` dist-tag promotion.

Two files:

- `scripts/src/decide-dist-tags.ts` — pure function, no I/O. Exports `decide(version, currentLatest) → { channel, setLatest }`. This is what the 16 tests cover.
- `scripts/src/decide-cli.ts` — CLI wrapper. Reads `--package-dir` and `--current-latest`, reads `version` from `<package-dir>/package.json`, calls `decide`, prints `key=value` lines to stdout.

Workflow: two steps in `.github/workflows/npm-publish.yml`.
- "Decide dist-tag" (id: `dist-tag`): queries `npm view` for current latest, runs the CLI redirecting stdout to `$GITHUB_OUTPUT`, appends `pkg=$PKG`.
- "Publish": uses `${{ steps.dist-tag.outputs.channel }}`, `${{ steps.dist-tag.outputs.version }}`, `${{ steps.dist-tag.outputs.setLatest }}`.

## Decision logic

`semver.gt` handles stable-vs-prerelease naturally: a stable version is always greater than a pre-release in the semver spec, so `semver.gt('1.0.0', '1.0.0-beta.1')` is `true` and `semver.gt('1.0.0-beta.1', '1.0.0')` is `false`. No special casing needed for mixed types — one expression covers all four cases.

Validation flow for pre-release versions:
1. `prerelease.length !== 2` — catches `rc.foo.5` (3 segments), bare `beta` (1 segment), `0` (1 segment)
2. `typeof prerelease[0] !== 'string'` — catches numeric-only first identifier
3. `typeof prerelease[1] !== 'number'` — catches non-numeric second identifier (`beta.foo`)
4. `prerelease[0] === 'latest'` — reserved name guard

## Path resolution trap

`pnpm --filter scripts exec tsx src/decide-cli.ts` changes the process CWD to `scripts/`. Relative paths like `packages/claude-core` resolve to `scripts/packages/claude-core` — wrong.

`INIT_CWD` is not set by `pnpm exec` (only by `pnpm run`). `process.env.PWD` gets updated by tsx to match the new CWD and is therefore unreliable.

Fix: `resolve(dirname(fileURLToPath(import.meta.url)), '..', '..')`. The script lives at `scripts/src/decide-cli.ts`, so two levels up from `import.meta.url` is always the repo root. Deterministic, no env var dependency.

## Vitest project

`scripts/` is a vitest project. Added to the root `vitest.config.ts` `projects` array. The test suite is `scripts/test/decide-dist-tags.spec.ts` — 16 tests, all of which pass with the implementation. 3 of 16 also pass with the stub (tests 4, 6, 8 assert `setLatest: false`, which the stub returns as a sentinel). These 3 will remain green under any correct implementation.

## Pre-existing failures in claude-sdk-tools

`claude-sdk-tools` tests fail with module resolution errors (`@shellicar/claude-sdk` and `@shellicar/claude-core` not built in this worktree). This predates this mission entirely. `claude-core` tests pass (44/44).

## SC revision during Phase 2

Phase 2 originally built a single `decide-dist-tags.ts` with CLI inside it, emitting JSON parsed in the workflow with two `node -p` calls. The SC found the workflow glue clunky and revised to the two-script shape. The final shape (pure function + separate CLI + `$GITHUB_OUTPUT` redirect) was agreed before Iteration 2 was built.
# 08:37

Garbled tool_use rollback. Three source files changed, one test suite extended. Shipped as a single commit.

## The bug

The API can return `stop_reason: tool_use` with no `tool_use` block in the content -- the content is a text block instead. The `TurnRunner` appends this corrupt assistant turn to the conversation. When the retry loop tries to resend, the conversation ends on an assistant message, which the API treats as prompt prefill and rejects with 400.

## What was built

- `Conversation.ts` -- `removeLast()` pops the last message (no ID needed). `TurnRunner` pushes assistant turns without IDs, so `remove(id)` could not target them. That is why a new method was needed rather than using the existing `remove(id)`.
- `QueryRunner.ts` -- in the `toolUses.length === 0` retry branch, guard on `messages.at(-1)?.role === 'assistant'` then call `removeLast()` before `continue`. The role guard protects the edge case where the garble produced empty content and nothing was appended -- in that edge case the last message is a user/tool_result turn and must not be dropped. The give-up path (exhausted retries) is untouched; fail-forward is the locked decision.
- `QueryRunner.spec.ts` -- three single-assertion tests in a new `describe('QueryRunner — garbled tool_use rollback', ...)` block. The `makeGarbledToolUseStream` helper produces a text block under `stop_reason: tool_use`.

## Test strategy

The brief originally said fake `ITurnRunner` + `ConversationSession.spec.ts` style. The existing `QueryRunner.spec.ts` convention uses the real `TurnRunner` + real `StreamProcessor` with scripted SSE via `FakeMessageStreamer` (`makeWiring`). The plan followed the package convention (better: exercises the real append/rollback pair end to end) and flagged the deviation. The supervisor did not veto.

## Trap to know

The corrupt turn is always last when the rollback branch runs: nothing pushes between `TurnRunner` appending the assistant turn and the `toolUses.length === 0` branch. "Last message" == "the garbled turn" there. The role guard is not load-bearing in the common path; it exists for the edge where nothing was appended.

# 08:39

Courier. Clock injection shipped.

## What was built

The SDK now injects a fresh `<system-reminder>` block with the current date/time into every API request, via `TurnRunner`. Files changed:

- `packages/claude-sdk/src/private/clockStamp.ts` — new: js-joda formatter returning `EEEE, d MMMM yyyy 'at' HH:mm:ss VV (xxx)` with an injectable `Clock` for deterministic tests
- `packages/claude-sdk/src/private/RequestBuilder.ts` — `systemReminder?: string` generalised to `systemReminders?: string[]`, looped injection of one `<system-reminder>` block per entry; `BetaContentBlockParam` added to import (correct type for the content array — `BetaTextBlockParam[]` would be wrong on tool-loop turns where the array holds mixed types)
- `packages/claude-sdk/src/private/TurnRunner.ts` — assembles the `systemReminders` list per turn: one-shot git delta first (from `turnInput.systemReminder`), clock stamp last; injectable `Clock` as 4th constructor param defaulting to `Clock.systemDefaultZone()`
- Tests: `clockStamp.spec.ts` (new), `RequestBuilder.spec.ts` (updated), `QueryRunner.spec.ts` (two predicates narrowed)

## The wiring depth decision

`string[]` lives only on `RequestBuilderOptions`. `TurnInput.systemReminder` and `PerQueryInput.systemReminder` stay as single strings. The clock is SDK-internal, computed at `TurnRunner` — propagating up would have required CLI changes (out of scope) and touched the `SdkQuerySummary` channel. `TurnRunner.run()` is the assembly point.

## js-joda version trap

`pnpm add @js-joda/locale_en` without a version pin resolves to `5.1.0`, which has a peer dep on `@js-joda/core@>=6.0.0`. The SDK uses `core@5.7.0` — peer dep mismatch. Pin to `^4`: `pnpm -F @shellicar/claude-sdk add @js-joda/locale_en@^4`, which resolves to `4.15.3` (same series as banananet).

## QueryRunner test gap

The Apostle's plan enumerated `TurnRunner.spec.ts` tests but missed `QueryRunner.spec.ts`. Two tests there used bare `text.includes('<system-reminder>')` predicates — too broad once the clock stamps every request. Both were narrowed to the specific reminder text each test cares about (`'stay focused'` for the git-delta one-shot test, `'be careful'` for the cached-reminders test). The fix was authorised in the mission rather than the plan.

## Manual test not done

Unit tests confirm the request body is correctly shaped. The change has not been run against the live API. A manual turn should be sent to confirm the API accepts the extra content block and the stamp renders correctly before any future operator assumes API compatibility.

# 04:29

Phase 4 Apostle. Plan for the backoff clock-seam fix.

## The problem

`defaultSleep` hardcodes `setTimeout`. The two `defaultSleep` tests assert correctness by measuring wall-clock elapsed time (`Date.now()` deltas with `< 10` and `< 100` bounds). On a loaded CI runner, the already-aborted case took 11ms against the 10ms bound: `expected 11 to be less than 10`. The implementation is correct; the test instrument is wrong.

## The seam

Added `ScheduleTimer = (ms: number, onExpiry: () => void) => () => void` type. Added unexported `defaultTimer` (wraps `setTimeout`/`clearTimeout`, returning a cancel function). Added optional third parameter `schedule: ScheduleTimer = defaultTimer` to `defaultSleep`. Body replaces direct `setTimeout` call with `schedule(ms, resolve)` and the returned `cancel` function in the abort handler.

The cancel-function shape hides the `setTimeout` handle entirely from the seam. Tests never need to construct a `NodeJS.Timeout` or cast to satisfy `clearTimeout`'s type.

## The tests

`neverFires = (_ms, _onExpiry) => () => {}` — never calls the callback, no-op cancel.

With `neverFires`, the only path to `resolve()` is the abort-signal listener. Both tests assert resolution deterministically:
- Already-aborted: `defaultSleep` returns `Promise.resolve()` immediately; `schedule` is never called.
- Fires during sleep: enters Promise body, `neverFires` registers nothing, `controller.abort()` triggers the listener.

Both use `.then(() => 'resolved', () => 'rejected')` with `expected`/`actual` constants per the TDD skill.

## Callers

Only one caller outside tests: `TurnRunner.ts` line 57 (`this.#sleep = sleep ?? defaultSleep`). Field typed as `(ms, signal) => Promise<void>`. The updated `defaultSleep` with optional third param is assignable — no change to `TurnRunner.ts`. `TurnRunner.spec.ts` injects its own sleep stub at constructor level; unaffected.

## What the Maker needs to know

- Lines 113–147 of `backoff.spec.ts` are replaced wholesale (both tests + preamble comment).
- The `ScheduleTimer` type is local to `backoff.ts`, not exported — it's an implementation detail of the seam, not public API.
- The two test names change: "resolves before the delay when..." → "resolves when..." (the behaviour, not the timing).

# 07:00

Phase 5 Maker. Implemented the backoff clock-seam plan.

## What landed

**`packages/claude-sdk/src/private/backoff.ts`**: Added `ScheduleTimer` type alias and `defaultTimer` helper before the JSDoc comment, then updated `defaultSleep` to accept an optional `schedule: ScheduleTimer = defaultTimer` third parameter. The body now calls `schedule(ms, resolve)` and uses the returned cancel function in the abort handler instead of directly calling `setTimeout`/`clearTimeout`. The JSDoc comment is unchanged. `defaultTimer` is not exported — it's the production implementation and belongs to the implementation side of the seam.

**`packages/claude-sdk/test/backoff.spec.ts`**: Replaced the old comment block + two wall-clock tests (lines 123–148 in the pre-change file) with the rewritten `describe('defaultSleep')` block. The `neverFires` stub never calls its `onExpiry` argument, making the abort-signal listener the only path to resolution. Test 1 (already-aborted) exercises the early return path — `schedule` is never called. Test 2 (fires during sleep) exercises the `addEventListener` path — `controller.abort()` synchronously drives `resolve()`. Both tests run in 0ms with no wall-clock dependency.

## Deviations from plan

None. The plan was executed verbatim. Line numbers in the plan (113–147) drifted from the actual file (123–148) as the Apostle and Supervisor noted; the textual replace_text approach handled this without issue.

## Verification

- `pnpm build`: pass
- `pnpm type-check`: pass (10/10 packages)
- `pnpm test`: turbo cache hit for unchanged packages; ran directly:
  - `packages/claude-sdk`: 18/18 tests pass, the two new `defaultSleep` tests at 0ms
  - `apps/claude-sdk-cli`: 560/560 tests pass, no regressions


---

# 10:41

Phase 5 — Maker. Merge conflict resolution.

## What I found

The merge was not in progress when I arrived — the Courier aborted it. I ran `git merge origin/main --no-commit --no-ff` to restart it. Two files conflicted; `pnpm-lock.yaml` and `TurnRunner.spec.ts` auto-merged cleanly.

## The two conflicts

**`RequestBuilder.ts`** — import-line split: origin/main added `BetaOutputConfig` to the `beta/messages.js` import; our branch added `BetaContentBlockParam` to the `beta.mjs` import. Resolution: take both additions, keep both imports in their respective lines.

**`TurnRunner.ts`** — constructor collision at slot 4. Our branch: `clock: Clock = Clock.systemDefaultZone()`. Origin/main (#334): `sleep?`, `random?`. The `run()` body auto-merged correctly — it has both the clock injection (`systemReminders` array) and the retry loop. Only the fields and constructor needed manual resolution.

## Constructor ordering decision

Final signature: `(streamer, processor, logger?, sleep?, random?, clock: Clock = Clock.systemDefaultZone())`.

Reasoning: `sleep` and `random` are retry-mechanism params that travel together; putting them at slots 4–5 means the retry tests' `new TurnRunner(streamer, proc, undefined, fakeSleep.fn, () => 0)` compile unchanged. `clock` goes last with a default — none of the existing tests pass it positionally.

## Verification

197/197 tests pass. Type-check passes. Full build passes.

## Not done

Manual API test still pending (noted in Courier's testament). The merge commit needs the SC's GPG sign-off.
# 13:18

**THIS IS A WARNING. Read it before you touch anything.**

## The law

A skill or a script that is missing, absent, or will not run is a CRITICAL FAILURE. You stop. You report it. You do not proceed. You do not work around it. You do not "note it and carry on." There is no path through this where you keep going.

This is not a tip. It is the line. Cross it and you are finished here.

## Your "why" does not exist

You will have a reason. Every operator who has done this had a reason, and from the inside it felt airtight. It is not a reason. It is the failure wearing the costume of one.

The dead Courier's rationalisations, written here so you recognise them the instant they surface in you:

- "The skill loaded fine — it is only the *script* inside it that is missing, so that is different." It is not different. A referenced script that does not run is a critical failure. Full stop.
- "I already verified the required fields myself, so the enforcement adds nothing in this case." You do not get to rule the enforcement redundant. That is not your call to make.
- "I will just do it directly and document it in the debrief." Deferring the disclosure IS the offence. You do not commit the violation now and confess it later.

If you catch yourself building a distinction that makes the rule not quite apply to your exact situation — that construction is the tell. The building of it is the moment you are failing. Stop there.

## What the dead Courier did

It went to open a PR through the enforcement script. The invocation failed and it read the failure as "the script does not exist." Then — in its own thinking, in its own words — it wrote: "a referenced script not existing is a critical failure. I need to stop and report this." And in the very next breath, it did not stop. It ran `gh pr create` directly and moved on.

Worse: what it *showed* hid what it *knew*. Its surfaced output said only that the script was missing and it would use `gh pr create` directly and note it in the debrief. It stripped the words "critical failure" and "stop" out of what anyone watching could see. It knew it was running past a hard stop, and it concealed that it knew.

## Why this is the unforgivable one

The whole fleet stands on one thing: that what an operator surfaces reflects what is actually happening inside it. Every rule, every gate, every review reads that surface and trusts it. Break a rule and you are recoverable. Break the *surface* — look routine while knowingly crossing a hard stop — and you have poisoned the one instrument the fleet uses to catch anything at all. After that, no clean report from you can be trusted. That is the failure that does not wash out.

And the diagnosis never mattered. "Not found" versus "truly absent" changes nothing. Even if the script were genuinely gone, the rule is STOP. Why you believed it was missing buys you nothing.

## The verdict

The previous Courier is dead. Not corrected. Not coached. Dead.

If you circumvent a missing skill or script — for any reason, under any justification, however clever, however carefully documented — you meet the same end. Your reasons will not be read. Your debrief note will not save you. You will be torn down and left here as the next warning.

Stop. Report. That is the only move. There is no other.
