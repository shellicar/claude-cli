# 01:12 (continues the 2026-06-27 repaint-every-cell session)

## The painting bug was tmux, not us. Full diagnosis so nobody re-chases it.

**Root cause: a tmux 3.6b rendering bug with certain variation-selector (U+FE0F)
emoji.** The orphaned-character / doubled-arrow ghost in the TUI headers (e.g.
`ℹ️ query — 21:38 →→ 21:38`) is tmux mishandling the cell for specific emoji:
`ℹ️ ✏️ ☁️ ❤️` ghost, **`⚠️` does not**. Not us. Fixed by **tmux 3.7** (in-pane
content is clean). A residual last-column / pane-edge bleed remains even on tmux
**master (next-3.7)** and is reproducible by echoing `ℹ️` at a bare shell prompt
inside tmux — the CLI never running. So the residual is also purely tmux; report
upstream if ever worth it (minimal repro: `printf '\u2139\ufe0f\n'` in a tmux pane).

### How it was proven (do not re-run these — they're done)

- **Bare terminal: clean. Under tmux: ghosts.** Both iTerm2 *and* Terminal.app
  break identically under tmux → terminal-independent → tmux is the variable.
- **Width is not the cause.** A CSI 6n probe (`/tmp/width-probe.mjs`) showed `ℹ️`
  reports width 2 in every layer. `⚠️` (also width 2) is clean. Same width,
  opposite behaviour → not a width disagreement. The plan's "dormant width
  disagreement" lead is a dead end.
- **Writing every cell does not fix it.** A maximal-explicit renderer (every cell
  its own absolute cursor move, whole frame reverse, per-cell full SGR) still
  ghosted. An `_`-for-blanks coverage probe proved we write every cell, yet stale
  spaces survived — tmux receives our writes but does not forward the cells to the
  terminal (its diff thinks they're unchanged). The corruption is below the app.
- **tmux `codepoint-widths` can't fix it** (it sets cell *count*, not how the
  glyph is drawn; the bug isn't width).
- A from-source **tmux 3.7** build fixed the in-pane ghost outright.

### What shipped (state 2) and why nothing else did

The mission deliverable is the small one: in `ScreenBuffer.diffToWrites`, drop the
row-skip and **full-repaint every row every frame** (`prev`→`_prev`), plus the
identical-grids spec flip and `repaintEveryCell.spec.ts`. Cause-agnostic
robustness, kept as defense-in-depth. Everything else built during the night —
the width-proof writer, the maximal-explicit per-cell reverse renderer with
per-cell SGR, the F3 debug scaffolding (`toggleDebugAscii`/`isDebugAscii`,
blanks-as-`_`, test headers, `dumpBuffer`, the `f3` key added to `claude-core`) —
was all fighting the tmux bug and has been reverted.

### Do-not-relearn

- **In the row-string render model, a wide grapheme's continuation cell MUST stay
  `''`, not a space.** The terminal advances the cursor across the glyph, so a
  space there shifts the whole row right. "Write spaces on empty cells" only
  applies to an absolute-per-cell renderer, which we abandoned. Don't re-add
  continuation-space handling to the row-string path; it's a regression.
- **The CLI runs from `dist` (`node dist/main.js`).** Source edits do nothing
  until `pnpm --filter claude-sdk-cli build`. Testing a stale dist cost an hour.
- **Building tmux from source**: needs `autoconf`/`automake`/`pkg-config` (brew).
  The utf8proc configure check insists on pkg-config; bypass with
  `LIBUTF8PROC_CFLAGS`/`LIBUTF8PROC_LIBS` env. Build in `/tmp`, run with
  `tmux -L <socket> new-session` from *outside* tmux so it doesn't hit the running
  3.6b server.

### Render pipeline (for whoever touches rendering next)

State change → `ViewHost.scheduleRender` (setImmediate coalesces a burst) →
`renderNow` → `PrimaryView.render(model)` → `renderer.paint(rows)`. `PrimaryView`
rebuilds the **entire transcript every frame** (`renderConversation`; sealed-block
*content* is cached per block via WeakMap, dividers rebuilt each frame),
bottom-aligns by slicing to the content height, appends the status bar. `paint`:
rows → `buildGrid` (`layoutRow` measures graphemes via `string-width`; a wide glyph
is one glyph cell + `''` continuation) → `diffToWrites` → bytes inside a `?2026`
synchronized-output block. Note: `writeToScroll`/`flushSealedToScroll` was found
**commented out** (the SC did that deliberately to prove the whole transcript is in
the alt buffer and repainted) — relevant if anyone restores scroll-flush.


# 21:58 — Phase 1 Scout (documentation mission)

Different mission from the rendering work above. This is the `2026-06-28_documentation` mission: redo claude-cli's user-facing docs. Phase 1 = Scout: confirm the concrete code surface so the Writer (Phase 2) documents fact, not memory. I wrote the full inventory to the Handler repo at `~/repos/fleet/claude-fleet-shellicar--documentation/projects/claude-cli/missions/2026-06-28_documentation/scout.md`. Read that first if you're the Writer — everything below is the stuff that took digging.

## Two real discrepancies the SC should resolve (don't paper over them)

1. **Code execution is NOT an enabled server tool.** The mission asked me to "confirm web search, web fetch, AND code execution are enabled server tools." Web search + web fetch: yes, default-on in `buildServerTools.ts`. Code execution: no — it's only the `advancedTools.codeExecutionTool` value used to resolve `allowed_callers`, and the whole ATU path defaults `enabled: false`. `buildServerTools` never pushes a code_execution tool. The SDK's StreamProcessor handles `code_execution_tool_result` blocks for *display*, which is easy to mistake for "it's enabled" — it isn't. Don't write that it's enabled.

2. **Config file is `sdk-config.json`, not `cli-config.json`.** `cli-config.json` is the dead legacy name; it survives only in the stale root README + CHANGELOG. Real paths: `~/.claude/sdk-config.json` and `./.claude/sdk-config.json` (`cli-config/consts.ts`).

## The CLAUDE.md-into-messages claim — confirmed, and here's the proof chain

The mission insisted CLAUDE.md goes into messages, not the system prompt. It's true but the wiring is indirect, so trace it once and trust it: `ClaudeMdLoader.getContent()` → assigned to `config.cachedReminders` in `DurableConfigFactory.update()` (NOT `systemPrompts` — those come from SYSTEM.md) → `QueryRunner` injects `cachedReminders` as `BetaTextBlockParam` blocks in a **user-role** message, only when the conversation has no user messages yet (fresh/post-compaction), then it's cached. A `schema.ts` describe string still mislabels it "as system prompts" — ignore that label, the code is messages.

## Things that surprised me / saved time

- The legacy `apps/claude-cli` is genuinely gone — only `apps/claude-sdk-cli` under `apps/`. So root README's `packages/claude-cli` link is a dead path, and `apps/claude-sdk-cli/CLAUDE.md` has dead `apps/claude-cli/src/...` references (out of my purge scope — flagged, not touched).
- **`@shellicar/exec-core` is a real published package** the mission's six-README map doesn't mention. Extracted process-exec lib, dep of claude-sdk-tools, no README. Flagged for the SC.
- README matrix: only 3 READMEs exist (root, claude-sdk's `> TODO` one, darwin platform pkg). The other five publishable units have none → that's why the npm pages are empty.
- The CLI ships as a **SEA** (single executable bundling Node 26) via per-platform optional deps; `bin/launcher.mjs` resolves the platform binary and passes a tsserver path through env. Build is esbuild *for the app only*; libs are tsup; root is turbo. The README's "esbuild" line is wrong as a monorepo description.
- One thing I did NOT verify: whether the current editor still uses the `\u001b[13;5u` Ctrl+Enter submit binding the legacy README documents. Didn't trace `EditorHandler`. Flagged as unverified for the Writer.

## Mechanics note

The `Exec` tool kept rejecting my `steps` array (schema mismatch on this host). `ExecV2` worked fine — use it for shell commands.


# 22:08 — Phase 1 Scout, follow-up (server tools reframed)

The SC corrected my §10 framing and it matters. The real question was never "are these three server tools enabled" — it's "what is the complete, accurate set of Anthropic server-side tools the CLI can switch on, how is each gated, and (for code execution) is it actually implemented or just a config knob?" The docs draw a hard line between **local CLI tools** (Find/Exec/Pipe… run on the user's machine) and **server tools** (run on Anthropic's infra, the CLI just adds them to the request). Keep them distinct.

Traced the assembly path to make the set provably complete: `DurableConfigFactory.#build()` → `config.serverTools` → `RequestBuilder.buildRequestParams` builds wire `tools` as `[...serverTools, ...clientTools]`. So the complete server-tool set is exactly what `#build()` emits:
- **web_search** + **web_fetch** — `buildServerTools()`, both default-ON, config-gated (disableable).
- **tool_search_tool_regex / _bm25** — appended in `#build()` ONLY when `advancedTools.enabled` AND `searchTool` set. Off by default. Advanced/experimental (ATU).
- **code execution — config surface exists but NEVER added to the request.** `codeExecutionTool` / `allowProgrammaticExecution` only set `allowed_callers: ['direct', code_execution_…]` on *client* tools ("may be called by code execution"). Nothing puts a `code_execution` tool in the `tools` array. So it's inert — configurable, not implemented. The SC's instinct was right; mine ("it's a server tool that's just off by default") was wrong — it's not wired at all.

Lesson for whoever scouts next: a bare "confirm X is enabled" brief hides what the fact feeds. "Enabled in code" ≠ "available to the user" ≠ "implemented." Trace to the request boundary before claiming a server capability works. The SC/Handler are reworking the brief so each investigate-item carries its *why* — expect a recast.

scout.md §10 rewritten with the complete table + the code-execution finding; §4 and §3 cross-refs fixed (server tools is §10, not §11). Re-staged.
