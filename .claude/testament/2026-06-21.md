# History view (#179)

A separate view for looking back over the active session's sealed blocks without disturbing the live one. Switch in with F2, navigate and inspect freely (including while a query is mid-stream), switch back with F1. Restores the in-session look-back that the move to an alt buffer took away.

## How it fits together

- **View switch — no refactor.** `ViewHost` already hosts and swaps presentations keyed by `AppModeKey`; both `dispatchKey` and `renderNow` route through `#activePresentation()`. The history view slots in as one additive `historyViewState` store subscription plus a new `'history'` presentation entry. The one gotcha: the presentations `Map` must be `new Map<AppModeKey, Presentation>([...])`. TypeScript infers the value type from the first entry, so two heterogeneous presentations (`PrimaryPresentation`, `HistoryPresentation`) clash on `#view` without the explicit generic.

- **Tool-data capture (additive, Primary untouched).** A sealed tools block in `ConversationState` was only a flat string of one-line summaries — per-tool input/output was dropped when display blocks were built. It is now captured alongside, leaving Primary's rendering byte-identical:
  - The SDK publishes a new `tool_result` message (`SdkToolResult`: id + content + isError) at every site in `QueryRunner.#handleTools` that builds a client `tool_result` block (not_found, invalid_input, handler_error, success, user-rejection). Content is **post-transform** by construction — the run closure holds the already-transformed string, so a ref-swapped large output shows as the ref the model saw.
  - `AgentMessageHandler` captures input at `tool_use_input_stop`/`server_tool_use` via `ToolObject.setInput`, output at `tool_result`/`server_tool_result` via `setOutput`. `#redrawTools` calls `conversation.setLastTools(content, entries)` — same content string as before, entries beside it.
  - `setLastTools` mirrors `setLastContent`'s active-then-sealed-reverse search. This matters: tool outputs land *after* `message_usage` seals the tools block (tools run between turns), so the sealed block is updated in place.
  - The Primary-unchanged guarantee is pinned by a characterisation test: identical event sequence with and without the output event, asserting byte-identical tools-block content.

- **Navigation is a box model.** Every block is a box (header divider + content). Collapsed, each box is capped (`COLLAPSED_CONTENT_LINES = 6`, ≈8 with the header) and `...` marks more; the focused box is centred on screen with neighbours bleeding off the top/bottom edges. `[open]` grows the focused box to its full content bounded by the screen; content taller than the screen scrolls inside a fixed frame (header stays, content slides by `scrollOffset`, `...` markers appear top/bottom only when content is hidden that direction). Tools nest the same model: open a tools block to its tool list, open a tool to its input/output.
  - `HistoryView.render` does it with `#blockCard` (builds each box), `#centre` (flattens cards, slices a screen-height window so the focused midline sits on the screen midline), `#contentBox` (returns lines whole when they fit, else a scrolled window).
  - **An opened tool's i/o renders as a labelled box** (`input`/`output` label + indented value through the window helper), *not* the illustration's inline `input { ... }`. The inline form can't scroll when output is tall, which the box-model rule requires. This is the one place faithfulness to the model outranks the literal glyphs.
  - Content scroll bottom-clamp needs the rendered (wrapped) height, and that measurement lives in the **model layer**, not the view. `model/blockLayout.ts` holds `blockContentLines(content, cols, indent, decorate)` — the single wrap walker — with a count-preserving `CodeDecorator` parameter (`plainCode` for measuring, the view's highlighter for painting); `historyOpenLines`/`historyContentBudget`/`historyContentExtent` sit there too. `HistoryView` renders through it and `HistoryNavHandler` imports `historyContentExtent` directly, so End/PgDn/Home land exactly at the box edge — no dead-zone, no over-scroll.

- **Keys are action-based.** `historyKeyMap(viewState, key) -> Action` is the pure state-aware translation; `HistoryViewState.apply(action)` mutates. Actions: `prev`/`next`, `open`/`close`, `scroll-up`/`scroll-down`, `page-up`/`page-down`, `home`/`end`. View select is direct and absolute: F1 → primary, F2 → history, reachable from any state, via `ViewSelectHandler`. F-key support was added to `KeyAction` in `claude-core`. Entry (F2 from outside history) focuses the latest block folded (`enterAtLatest`); no focus state is kept across exits — re-pressing F2 while in history leaves focus alone. Moves clamp at boundaries (no-op, no emit).

## Decisions the SC made through the arc

The navigation design was reworked from the ground up more than once: a one-line-row outline, then a fold/unfold collapsible outline, then the box model that shipped. View switching went from an enter/exit toggle to direct F1/F2 absolute select. A footer view bar (active view marked accent + underline, inactive dimmed) was added as host-level chrome. Tool output capture was settled as post-transform (a faithful mirror of what the model saw, not a file viewer). The shipped code reflects the final state of all four; the mission brief carries the full history if needed.

## Traps for the next operator

- **The plan's file:line citations are offset** (written against `f1f3b6e`) by a handful of lines throughout. Every cited construct exists — re-ground each call site before editing rather than trusting line numbers.
- **turbo caches `pnpm type-check`** (`FULL TURBO`) and won't re-run `tsc` on new source when the working tree equals the index. Trust a direct `pnpm exec tsc --noEmit` in the package, not the cached turbo result. Same for `TsDiagnostics` — it can report stale; the direct `tsc` is the gate.
- **Single-spec runs fail** in `apps/claude-sdk-cli` (documented vite workspace-resolution failure). Run `pnpm test` from the repo root.
- **Testaments ship with the PR.** `.claude/*/**/*.md` is whitelisted in this repo's `.gitignore`, so this file is on the branch.
- **tdd lesson worth keeping:** assert the produced value (the rendered rows array, exact strings) — never fold it to a boolean (`includes(...).toBe(true)`), which names no value on failure. The render-level frame tests pin the box-model format against the Navigation illustrations.
- **Measurement is model-layer work, not view work** — and `controller/biome.json` enforces it (`controller/` must not import `view/` or `app/`; `pnpm lint` is the gate). The first cut put the clamp height in a `view/historyContent.ts` and reached for it from the handler, which the gate rejected. The honest fix split `renderBlockContent` into *layout* (row count, now `model/blockLayout.ts`) and *decoration* (cli-highlight, stays in the view). The decorator contract is count-preserving — highlighting paints inside a row, never changes the count — so measuring with `plainCode` and rendering with the highlighter yield the same height. `view/historyContent.ts` no longer exists; do not resurrect it.
- **Container `.to(X)` vs `.to(X, () => new X())`** — core-di-lite makes these identical for a zero-arg constructor (`createInstance: factory ?? (() => new impl())`), and field deps use the `dependsOn` decorator, not constructor reflection. The redundant factories were reduced to `.to(X)`; the load-bearing ones (a constructor-injected service, or a hand-passed value like `nodeFs`/config) were left alone. `buildProvider()` eagerly resolves every registration, so a bad reduction throws at container build.

## One deferred call

The view bar's exact footer row-sharing with command-mode hints is the one layout point the brief left open: `commandRow || viewBar` fills the row when no hint shows. Settle the both-present case if it ever surfaces.
# 05:20

Apostle — messages-api-layer, Phase 2, iteration 1. Produced the build plan at the mission dir in the fleet repo: `projects/claude-cli/missions/2026-06-15_messages-api-layer/plan.md`. Read-only in this worktree; only this testament written here.

## The decisions that shaped the plan

- **Boundary = investigation Option C.** Owned `IMessageStream` interface carrying the exact subset `StreamProcessor` consumes (6 consumer events + `finalMessage` event + `finalMessage()`). `StreamProcessor`'s body is untouched; only the parameter type moves `BetaMessageStream` → `IMessageStream`. Event payload types stay SDK types (kept per the plan).
- **The partial-JSON parser is NOT needed.** This is the load-bearing simplification. `StreamProcessor.inputJson` forwards the raw delta *string* (`StreamProcessor.ts:104`), and parsed input is only read at `contentBlock`/`finalMessage`. So the owned accumulator buffers `input_json_delta` strings and `JSON.parse`s once at `content_block_stop`. The investigation called accumulation "the real measure of the work"; this cuts it down a lot.
- **Error classes = owned (option ii).** Defined `HttpError`/`ApiStreamError`/`ConnectionError`/`TimeoutError` and rewrote `backoff.ts` `isRetryable` against them, removing the last SDK runtime import. Abort is `signal.reason` (DOMException), handled by an `abortSignal.aborted` check before classification — no `APIUserAbortError` equivalent.
- **Retry stays in `TurnRunner`.** It already owns the loop + clock + sleep + random. Keeping it there is what lets `QueryRunner` and the control channel stay unchanged (a design constraint). Added: capped account-limit branch, 10-min give-up budget (checked *before* the wait), separate transient counter so account-limit waits don't consume `MAX_RETRIES`.
- **X/Y bypass `SdkMessage` entirely.** A new `AccountLimitListener` (SDK type) injected into `TurnRunner`, implemented CLI-side by `AccountLimitNotice` driving the existing `ConversationState.spliceNotice`. This is the only way to surface X/Y without touching `QueryRunner` or the control channel (both locked).

## Wire constants — grounded, not invented

From installed `@anthropic-ai/sdk@0.92.0`: base URL `https://api.anthropic.com` (`client.mjs:54`), `anthropic-version: 2023-06-01` (`:476`), endpoint `POST /v1/messages?beta=true` + `stream:true`, retry-after parsing `retry-after-ms` then `retry-after` sec/HTTP-date (`:388-407`), streaming timeout 600000ms, SSE framing on double-newline (`core/streaming.mjs`). Deliberately dropped the `X-Stainless-*`/platform telemetry headers — flagged.

## Friction surfaced (did NOT paper over)

The sharp one: **give-up shows 🛑 AND a generic `[error: HTTP 429]`** because `QueryRunner` (locked) renders every thrown error. The worked example shows only 🛑. Presented two options (accept it / a 2-line CLI guard) without picking — suppression is a real behaviour change. Also flagged: "clear the notice" interpreted as gate-reset not scrollback-erase; "refresh not a new line" met by once-per-episode splice gating; "applies to all 429s" resolved per the mission's own note (only capped 429s take the account-limit path); the `TurnRunner` 4th-param seam shift.

## For the next operator (Scaffolder/Builder)

The plan's §9 is the Red target. Highest-value test is `MessageStream` end-to-end against a recorded SSE fixture feeding the real `StreamProcessor` — that proves the accumulator+SSE+boundary together. The accumulator field names (`delta.container`, `delta.context_management`, `event.usage`) need checking against the 0.92.0 d.ts via `TsHover`; only `stop_reason`/`context_management`/`usage.*` are load-bearing (what `StreamProcessor` reads off the final message). Verify whether `index.ts` already re-exports `public/types` before adding an `AccountLimitListener` barrel line.


# 09:16

Apostle — Phase 2 **iteration 2**. Mission reopened: `QueryRunner` moved into scope and a new design decision "The give-up must be coherent — one signal, not two." This resolves the exact friction I flagged in iteration 1.

## What changed in the plan

The give-up no longer throws a raw 429 for `QueryRunner` to render as `[error]`. New mechanism:
- `TurnRunner` give-up branch raises `stopped()` (🛑, via the listener) and throws a **dedicated** `AccountLimitStoppedError` (new owned error in `http/errors.ts`) — not the raw 429.
- `QueryRunner`'s turn-loop catch gains one clause: `if (err instanceof AccountLimitStoppedError) return;` — ends the query cleanly, no `error` emit. All other errors keep the existing `{type:'error'}` path.
- `AgentMessageHandler` untouched — no display guard. The SC explicitly ruled a display-layer guard a hack, so the fix lives in the termination path (where the error is never created), not the display path (where it would be hidden).

Why the listener still carries 🛑 (not `QueryRunner` emitting on the channel): keeps ⏳ and 🛑 on the same notice mechanism (`AccountLimitNotice` → `spliceNotice`), and avoids adding an `SdkMessage` variant / touching the control channel (both still locked). `QueryRunner`'s scope-inclusion is purely the clean *termination*, which maps to the decision's wording "the error/termination path, QueryRunner included."

## Edits made (sections touched, rest stood)

Top scope note; §3 (add `AccountLimitStoppedError`); §4 (give-up throws the sentinel + import); §5 (replaced the friction paragraph with the coherent-stop design + `QueryRunner` diff); §8 (QueryRunner now Modified, removed from Untouched, errors.ts bullet); §9 (new `QueryRunner` give-up test group, TurnRunner give-up test now asserts `AccountLimitStoppedError`); Decisions surfaced #1 rewritten as resolved. Decisions #2–#5 (ESC "clear" meaning, refresh-gating, "all 429s" reading, dropped telemetry headers, constructor seam shift) still stand.

## For the Maker

`QueryRunner` imports `AccountLimitStoppedError` from `./http/errors`. The sentinel extends `Error` (not `TransportError`) so `isRetryable` never matches it. The give-up test is a clean classicist QueryRunner test: fake `TurnRunner` throws the sentinel → assert the recording publisher saw no `error`.


# 10:28

Apostle — Phase 2 **iteration 3**. Mission reopened with two directions (found by re-reading the mission + Delivery Notes; the Handler didn't name them):
1. §2: re-examine the boundary choice on *which is the smaller change*; the SC expects NOT reimplementing the SDK's MessageStream events is easier.
2. §9: the SSE parsing must get **direct** unit tests, not just exercise-through-the-stream.

## The boundary re-decision (the substantive change)

I re-examined and agree: **switched from Option C (reproduce the emitter) to Option B (raw-event iterable + assembly in StreamProcessor).** It is genuinely less code. Option C built a `MessageStream` object that translated raw events → 6 typed events, which `StreamProcessor` then re-read — a redundant hop. Option B deletes that whole layer:
- Boundary is now `IMessageStream = AsyncIterable<BetaRawMessageStreamEvent>`.
- Transport is one async generator `streamMessages()` (~40 lines): fetch + bearer + SSE, throws connect/mid-stream errors, `yield* parseSse`. No emitter, no finalMessage promise plumbing.
- `StreamProcessor.process` rewritten as one `for await` loop over raw events, folding in the (retained, testable) `MessageAccumulator`. It already read raw events via its `streamEvent` handler, so this is a natural consolidation — same emits, same order, source changes from pre-translated to raw.
- `AnthropicClient` loses its EventEmitter entirely (simpler).

**The one real consequence I surfaced:** the thin transport no longer accumulates, so audit's `finalMessage` BetaMessage now comes from `StreamProcessor`'s new `final_message` event. main.ts moves the audit subscription from `client.on('finalMessage')` to `processor.on('final_message')` (1 line); `private/types.ts` gains a type-only `BetaMessage` import. AuditWriter unchanged.

## What carried over unchanged

§3 errors (incl. AccountLimitStoppedError), §4 retry/give-up loop, §5 give-up coherence + QueryRunner catch + AccountLimitNotice, §6 auth, §7 deletions — all stand from iteration 2. Also fixed the stale §4 sentence the iteration-2 supervisor flagged ("keeps QueryRunner... untouched" — no longer true).

## §9 SSE tests

Added a dedicated `parseSse` test group: frame split across reads, multiple frames in one buffer, multi-line data, ping skip, comment ignore, error-frame throw, **trailing partial flushed**, CRLF delimiters. To support the trailing-partial test I added a flush-after-loop step to `parseSse` (parse remaining buffer if non-empty at stream end).

## Method note (rewrite, not patch)

The rework touched §1, §2, §8, §9 substantially plus ripples, so I overwrote the whole plan file rather than ~15 fragile interleaved textEdits. The standing sections were carried forward verbatim.

## For the Maker

The boundary is now an async iterable — `StreamProcessor` does `for await (const event of stream)`. Error propagation is implicit (a throw from the generator rejects `process()`); no promise plumbing. The highest-value test remains the `StreamProcessor` end-to-end one, now driven by an async iterable of raw events (recorded fixture). `parseSse` flush-at-end handles truncated streams; the accumulator field checks (`delta.container` etc.) still need `TsHover` against the 0.92.0 d.ts.
