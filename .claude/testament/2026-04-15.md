# TypeScript Language Tools (#263)

Branch: `feature/ts-tools`. Four tools: `ts_diagnostics`, `ts_hover`, `ts_references`, `ts_definition`.

## tsserver protocol

tsserver uses custom framing, not LSP JSON-RPC. Outbound: `JSON.stringify({ seq, type: "request", command, arguments }) + "\n"` on stdin. Inbound: `Content-Length: N\r\n\r\n{json}` on stdout. Buffer and parse incrementally because responses arrive in chunks. Match responses to requests via `seq`/`request_seq`. Ignore messages with `type: "event"`.

All position-based commands take `{ file, line, offset }` where both `line` and `offset` are 1-based. The interface uses `character` externally, mapped to `offset` internally.

A file must be `open`ed with tsserver before any query. No delay is needed after `open`: the sync commands (`semanticDiagnosticsSync`, `quickinfo`, `references`, `definition`) all block internally until the project graph is ready. The POC had a 500ms delay here which was unnecessary and has been removed.

## Command-to-response mapping

| Command | Response shape | Key fields |
|---------|---------------|------------|
| `semanticDiagnosticsSync` | `Diagnostic[]` | `start.line`, `start.offset`, `text`, `code`, `category` |
| `quickinfo` | object or fail | `displayString` (→ `text`), `documentation` (empty string → undefined), `kind` |
| `references` | `{ refs: [...] }` | `ref.file`, `ref.start.line`, `ref.start.offset`, `ref.lineText` (→ `text`) |
| `definition` | array directly (not wrapped) | `span.file`, `span.start.line`, `span.start.offset` |

The `references` response wraps results in `{ refs }`. The `definition` response is a bare array. This inconsistency is worth knowing because it changes how you extract the body.

## `createRequire` for tsserver resolution

Cannot use `import.meta.resolve` because tsserver is not the package entry point. `createRequire` resolves `typescript` to find the lib directory, then derives `tsserver.js` from there. The consumer must have `typescript` installed.

The initial POC used `createRequire(path.resolve(cwd, '__placeholder.js'))` which breaks when `cwd` has no `node_modules`. The fix uses `createRequire(import.meta.url)` so resolution starts from the package itself.

## Test fixture

`test/fixtures/ts-project/` has `tsconfig.json`, `greeter.ts` (class with JSDoc, `prefix` property, `greet` method), and `main.ts` (imports Greeter, has an `_unused` variable for "no references" test). Tests start a real `TsServerService` per suite.

Fixture files needed biome compliance: `public` modifiers on class members, `_unused` underscore prefix, `biome-ignore` for `console.log`.

## CLI wiring

`TsServerService` is created once in `main.ts` with explicit `cwd` (required), started eagerly at app init (alongside config watcher), and stopped in the cleanup handler before watcher dispose and layout exit.

The four tools are factory functions that take the shared service instance. They go into `otherTools` which feeds `ToolRegistry`. No changes needed outside `main.ts`.

## Package exports

Each tool needs a `./TsXxx` export in `package.json` (import + require, types + default). Used `pnpm pkg set` for each. Entry files in `src/entry/` are picked up by tsup automatically.

## Biome traps

**`noBarrelFile`** triggers on `export ... from` syntax. The fix is to import individually, then export the imported binding. Every entry file in this package follows this pattern.

**`noConsole` override** at the repo root uses `scripts/**` which only matches the root `scripts/` directory. Package-level `scripts/` directories (like `packages/claude-sdk-tools/scripts/`) are not covered. The POC script was deleted rather than worked around.

**Import ordering**: biome's `organizeImports` sorts `type` imports before value imports from the same source, and alphabetises by specifier path. Adding a new import between existing sorted imports will trigger the organizer. Run biome on modified files before committing to avoid a pre-push rejection.

**`useConsistentMemberAccessibility`**: this codebase requires explicit `public` on all class members including constructors and abstract methods. The rule is set to error level.

## TsService entry point

Shared types (`ITypeScriptService`, `Position`, etc.) and the `TsServerService` class are exported via `./TsService`, not via `./TsDiagnostics`. The original design put everything in the TsDiagnostics entry which triggered `noBarrelFile`. Splitting the shared infrastructure into its own entry avoids the barrel file issue and is a cleaner API boundary.


# 08:56

## Task: Create `packages/mcp-exec` (#268)

New package wrapping the `claude-sdk-tools` Exec tool as an MCP server.

### What was built

`packages/mcp-exec/` with:
- `src/entry/index.ts` - `createExecServer()` function (MCP server setup)
- `src/entry/cli.ts` - binary entry point, imports from `./index.js`
- `tsconfig.json`, `tsconfig.check.json`, `tsup.config.ts`
- `package.json` created via `pnpm init` + `pnpm pkg set` + `pnpm add`

### Key design decisions

**Why `Exec.input_schema` directly, not `.shape`**: The MCP SDK's `registerTool` accepts `AnySchema` (a full ZodObject), not just a raw shape. Passing `Exec.input_schema` directly lets TypeScript infer the handler input type as `z.output<typeof ExecInputSchema>`, which is exactly what `Exec.handler` expects. Using `.shape` required a cast and caused an implicit `any` error.

**Why no `outputSchema`**: `ExecOutputSchema` is not exported from `@shellicar/claude-sdk-tools/Exec`. Defining it inline would duplicate the type. Since `outputSchema` is optional in the MCP SDK, leaving it out is clean.

**Why `cli.ts` imports from `./index.js`**: The biome `noBarrelFile` rule fired on the original `index.ts` which just re-exported from `../createExecServer.js`. Moving the implementation into `src/entry/index.ts` directly and having `cli.ts` import from `./index.js` satisfies the rule. Both are tsup entry points; tsup handles cross-entry imports via shared chunks.

**tsup split**: ESM entry includes both `cli.ts` and `index.ts`. CJS entry is `index.ts` only - the CLI binary only makes sense as ESM, and avoiding a CJS `cli.cjs` is cleaner.

**`pnpm pkg set` gotcha**: Setting exports with `.` as the key is tricky. Shell quoting `'exports[.].import.types=...'` (bracket notation without inner quotes) works correctly - pnpm parses `[.]` as the literal key `.`. `exports.'.'.xxx` sends `'.'` as the key (with quotes as part of the key name). `exports["."].xxx` sends `"."` (with escaped quotes). Only the bare `[.]` form works.

**`pnpm pkg set private=false`** sets the value as string `"false"` (truthy), not boolean. Use `pnpm pkg delete private` instead - a public package without `private` field is public by default.

### What works
- Build: `pnpm turbo build --filter=@shellicar/mcp-exec` passes
- Type-check: `pnpm turbo type-check --filter=@shellicar/mcp-exec` passes
- Biome: clean on all source files
- The MCP server correctly wraps `Exec.handler` - execution logic lives entirely in `claude-sdk-tools`
