# 01:50

Phase 2 — Builder. Fable-5 support: implemented the data and rendering the scaffold's tests pin.

## What was built

Four changes, exactly as the scaffold's test contract specified, nothing more:

1. `packages/claude-sdk/src/private/pricing.ts` — `PRICING` gains a `claude-fable-5` entry: `{ input: 10/M, cacheWrite5m: 12.5/M, cacheWrite1h: 20/M, cacheRead: 1/M, output: 50/M }`. Placed first in the record (newest flagship). Each rate divides by `M` (1_000_000) like every existing entry, so 1M tokens of a kind yields the plain dollar figure the tests assert.
2. Same file — `CONTEXT_WINDOW` gains `'claude-fable-5': 1_000_000`, placed first. This is requirement 2's named data. It would also resolve via the family default below, but the mission frames this as "data, not functionality", so the explicit entry is the right shape, not redundant defence.
3. Same file — `getFamilyDefault` regex widened from `/^claude-(opus|sonnet)-/` to `/^claude-(opus|sonnet|fable)-/`. This is requirement 3: an unmapped fable id (`claude-fable-6`) fails forward to the 1M family minimum.
4. `apps/claude-sdk-cli/src/view/renderStatus.ts` — `formatTokens` gains an M-suffix branch ordered before the existing k-branch: `n >= 1_000_000` renders `X.XM`. Order matters: M must be checked before k or 1M would render as `1000.0k`.

No tests were modified. The implementation made them pass unchanged, which is the signal the scaffold was correct.

## Environment gap hit (important for the Courier)

First verify run: `pnpm build` passed (turbo cache hits) but `pnpm type-check` FAILED. Every error was in `src/entry/main.ts` and `src/setup/container.ts`, root cause `Cannot find module '@shellicar/core-di-lite'` with a cascade of implicit-any errors downstream of the unresolved import. None in the files I touched.

Diagnosis: `node_modules/@shellicar` did not exist in this worktree — workspace deps were not installed for this cast. `core-di-lite` (added to the tree by `b05b4ed`, the commit the scaffold sits on) was the one missing package. `pnpm-lock.yaml` already referenced it (3 hits), so the lockfile was correct; node_modules just had not been materialised.

Fix: `pnpm install --frozen-lockfile`. Reported "Lockfile is up to date", added 1 package (core-di-lite), did NOT modify the lockfile. Re-ran verify: all clean. So there is no lockfile change to stage — only the two source files.

If the Courier's push triggers CI on a fresh checkout, CI runs its own install and this gap will not recur there. It was specific to this worktree's node_modules state.

## Verify state (all from repo root unless noted)

- `pnpm build`: FULL TURBO, 6/6.
- `pnpm type-check`: 10/10 tasks pass.
- `pnpm test`: 11/11 tasks pass.
- Direct run, `pricing.spec.ts`: 18/18 pass (was 11 pass / 7 fail before the build).
- Direct run, `renderStatus.spec.ts`: 40/40 pass (was 38 pass / 2 fail before the build).
- All 9 previously-failing scaffold tests now pass.

## Staged files

```
M  apps/claude-sdk-cli/src/view/renderStatus.ts
M  packages/claude-sdk/src/private/pricing.ts
```

Suggested commit message: `Report cost and context window for Claude Fable 5`
