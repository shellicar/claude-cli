# 00:38

Apostle cast for the core-di-lite introduction. Plan written to the PM repo at `projects/claude-cli/investigation/2026-06-07_core-di-lite-plan.md`. No code landed in the target repo.

## Branch state

`feature/introduce-core-di-lite` — 0 ahead/behind `origin/main`. Clean at start (two unstaged testament files from prior operators; the SC discarded them before I proceeded).

## The central finding

`main.ts` has grown to 501 lines (the May survey said 326; #339 AppLayout split added ~175 lines of resolved services imported and wired). The construction-to-activation ratio is roughly 4:1 — most of `main()` is `new X(...)` calls. The container boundary is clean.

## What the package looks like

`@shellicar/core-di-lite` v1.0.2 is published but not yet in `package.json`. Found the source in the pnpm store (`/Users/stephen/Library/pnpm/store/v10/projects/4a77018b...`). Key facts:

- `createServiceCollection()` → `IServiceCollection`
- `.register(Identifier).to(Implementation)` or `.to(Identifier, factory)`
- `.buildProvider()` — eager, synchronous, resolves everything in dependency order
- `provider.resolve(Identifier)` — returns the singleton
- `@dependsOn(Identifier)` field decorator for property injection
- Throws `DuplicateRegistrationError` on duplicate registration, `CircularDependencyError` on cycles
- Registration order does not matter (DAG-sorted at build time)

## The split I drew

**Container (composition):** ~35 services. Everything that can be constructed synchronously without network or async activation. `configLoader.load()` is synchronous I/O so it's called before `buildContainer` (pre-loaded instance passed in), keeping the container purely synchronous.

**Stays in `main()` (activation):** `await auth.getCredentials()`, `await tsServer.start()`, session init (`resume/startFresh/load`), `configLoader.start()` (file watcher), signal handlers, event wiring (`client.on`, `processor.on`, channel subscriptions), `renderer.enter()`, history replay, turn loop.

## New classes and why

**`SdkChannel` / `ConsumerChannel`**: `ControlChannel<SdkMessage>` and `ControlChannel<ConsumerMessage>` are the same generic class. The container uses the class reference as identifier; registering the same class twice would throw `DuplicateRegistrationError`. Named subclasses are zero-cost.

**`ModelOverrides`**: Wraps the mutable `{ model, thinking, effort }` overrides object and implements `ModelSettings`. Depends on `StatusState` for `cycleThinking()`/`cycleEffort()` calls. The `getEffectiveModel()` computation lives in `DurableConfigFactory` (not here) to keep `ModelOverrides` free of `ConfigLoader` as a dep.

**`AppToolsService`**: Wraps `createAppTools()` so the three outputs (`tools`, `store`, `refTransform`) are one injectable singleton. Both `ToolRegistry` and `AgentMessageHandler` need parts of it.

**`DurableConfigFactory`**: Replaces the `mapConfig()` closure. Owns the shared mutable `DurableConfig` reference. `QueryRunner` and `AgentMessageHandler` both receive `factory.config` — the same object. `factory.update(claudeMdContent)` is called in `runTurn()` before each turn, mutating the shared reference in place. This preserves the per-turn mutation pattern without either consumer needing re-injection.

**`container.ts`**: The composition root function. Takes `{ configLoader, modelOverride }`. Returns `IServiceProvider`.

## ApprovalNotifier constructor change

`ApprovalNotifier` previously took `ApprovalNotifyConfig | null`. Changed to `ConfigLoader` with lazy read at `start()` time. Reason: `configLoader.config` is available at build time (pre-loaded), but lazy access is the correct pattern regardless — the notifier should read live config, not a snapshot. Five tests in `ApprovalNotifier.spec.ts` change their construction call; behaviours identical.

## Stop note: `ReadLine` ordering

`ReadLine`'s constructor sets `process.stdin.setRawMode(true)`. If the container resolves `ReadLine` eagerly at `buildProvider()`, stdin enters raw mode before `renderer.enter()`. Current code constructs `ReadLine` after `renderer.enter()`. Plan flags this as Q4 — if ordering causes visible artifacts, `ReadLine` stays outside the container.

## Nothing invented

All decisions with genuine forks are surfaced as open questions (Q1–Q6), not resolved by guesswork. The plan is complete and implementable as written.


# 05:33

Maker cast for the core-di-lite introduction. Phase 2 complete. All changes staged, not committed.

## What landed

**New files (6)**:
- `src/setup/SdkChannel.ts` — named `ControlChannel<SdkMessage>` subclass for DI identity
- `src/setup/ConsumerChannel.ts` — named `ControlChannel<ConsumerMessage>` subclass for DI identity
- `src/setup/ModelOverrides.ts` — replaces the three-field `overrides` plain object and five inline cycle functions in `main()`. Implements `ModelSettings`.
- `src/setup/AppToolsService.ts` — wraps `createAppTools()` so `tools`, `store`, `refTransform` are one injectable singleton
- `src/setup/DurableConfigFactory.ts` — replaces `mapConfig()` closure. Owns the shared mutable `DurableConfig` reference. Imports `logger` from `../logger.js` (module singleton; plan's code block omitted this import — added it)
- `src/setup/container.ts` — composition root. All ~35 `new X(...)` constructions moved here.

**Modified files**:
- `apps/claude-sdk-cli/package.json` — added `@shellicar/core-di-lite: ^1.0.2` dependency; `pnpm install` ran
- `src/model/ApprovalNotifier.ts` — constructor now takes `ConfigLoader<any>` instead of `ApprovalNotifyConfig | null`; reads `this.#configLoader.config.hooks.approvalNotify` lazily
- `src/entry/main.ts` — all construction removed; `buildContainer()` called after `configLoader.load()`; activation code uses `provider.resolve(...)` throughout
- `test/ApprovalNotifier.spec.ts` — added `fakeConfigLoader()` helper, replaced all construction args
- `test/AgentMessageHandler.spec.ts` — fixed `new ApprovalNotifier(null, ...)` with inline fake config loader (plan §5 said "Unaffected" but constructor change rippled here)

## Deviations from plan

1. **`ConfigLoader<T>` generic arg**: Plan wrote `ConfigLoader` bare in type annotations. `ConfigLoader<T extends z.ZodType>` requires a type arg. Used `ConfigLoader<any>` throughout — consistent with Q1's note that config is effectively `any` due to Zod 4 widening.

2. **`import type { ConfigLoader }` in `container.ts`**: Plan used `import type` but `services.register(ConfigLoader)` and `x.resolve(ConfigLoader)` require it as a value. Changed to value import.

3. **Setup class imports in `main.ts`**: Plan showed `import type` for setup classes but `provider.resolve(SdkChannel)` etc. require them as values. Changed to value imports.

4. **Missing SDK imports in `main.ts`**: Plan's diff only added `QueryRunner` from `@shellicar/claude-sdk`. Activation code also needs `AnthropicAuth`, `AnthropicClient`, `ApprovalCoordinator`, `Conversation`, `SdkMessage`, `StreamProcessor`.

5. **Missing `logger` import in `DurableConfigFactory.ts`**: Plan's code block omitted it. Added.

6. **`AgentMessageHandler.spec.ts` impacted**: Plan §5 said "Unaffected" but `new ApprovalNotifier(null, ...)` broke. Fixed inline.

7. **`conversation` and `conversationState` as locals in `main()`**: Plan §6 mentioned these become `provider.resolve(...)` calls but the diff didn't show explicit local assignments. Added them before the historyReplay block.

8. **Duplicate `Conversation` import in plan's `container.ts`**: Consolidated.

9. **Unused `ITypeScriptService` import in `AppToolsService`**: Dropped.

## Gates

`pnpm run type-check`: PASS (0 errors)
`pnpm run test`: PASS (674 tests / 41 files)
`pnpm run build`: PASS

## Q4 outcome

The `ReadLine` raw-mode risk: resolved `ReadLine` before `renderer.enter()`, matching the plan's diff exactly. If stdin raw mode before alt-buffer causes visible artifacts, this is the place to look.