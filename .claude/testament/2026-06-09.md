# 00:38

Apostle cast for the core-di-lite introduction. Plan written to the PM repo at `projects/claude-cli/investigation/2026-06-07_core-di-lite-plan.md`. No code landed in the target repo.

## Branch state

`feature/introduce-core-di-lite` — 0 ahead/behind `origin/main`. Clean at start (two unstaged testament files from prior operators; the SC discarded them before I proceeded).

## The central finding

`main.ts` has grown to 501 lines (the May survey said 326; #339 AppLayout split added ~175 lines of resolved services imported and wired). The construction-to-activation ratio is roughly 4:1 — most of `main()` is `new X(...)` calls. The container boundary is clean.

## What the package looks like

`@shellicar/core-di-lite` v1.0.2 is published but not yet in `package.json`. Found the source in the pnpm store (`/Users/stephen/Library/pnpm/store/v10/projects/4a77018b...`). Key facts:

- `createServiceCollection()` → `IServiceCollection`
- `.register(Identifier).to(Implementation)` or `.to(Identifier, factory)`
- `.buildProvider()` — eager, synchronous, resolves everything in dependency order
- `provider.resolve(Identifier)` — returns the singleton
- `@dependsOn(Identifier)` field decorator for property injection
- Throws `DuplicateRegistrationError` on duplicate registration, `CircularDependencyError` on cycles
- Registration order does not matter (DAG-sorted at build time)

## The split I drew

**Container (composition):** ~35 services. Everything that can be constructed synchronously without network or async activation. `configLoader.load()` is synchronous I/O so it's called before `buildContainer` (pre-loaded instance passed in), keeping the container purely synchronous.

**Stays in `main()` (activation):** `await auth.getCredentials()`, `await tsServer.start()`, session init (`resume/startFresh/load`), `configLoader.start()` (file watcher), signal handlers, event wiring (`client.on`, `processor.on`, channel subscriptions), `renderer.enter()`, history replay, turn loop.

## New classes and why

**`SdkChannel` / `ConsumerChannel`**: `ControlChannel<SdkMessage>` and `ControlChannel<ConsumerMessage>` are the same generic class. The container uses the class reference as identifier; registering the same class twice would throw `DuplicateRegistrationError`. Named subclasses are zero-cost.

**`ModelOverrides`**: Wraps the mutable `{ model, thinking, effort }` overrides object and implements `ModelSettings`. Depends on `StatusState` for `cycleThinking()`/`cycleEffort()` calls. The `getEffectiveModel()` computation lives in `DurableConfigFactory` (not here) to keep `ModelOverrides` free of `ConfigLoader` as a dep.

**`AppToolsService`**: Wraps `createAppTools()` so the three outputs (`tools`, `store`, `refTransform`) are one injectable singleton. Both `ToolRegistry` and `AgentMessageHandler` need parts of it.

**`DurableConfigFactory`**: Replaces the `mapConfig()` closure. Owns the shared mutable `DurableConfig` reference. `QueryRunner` and `AgentMessageHandler` both receive `factory.config` — the same object. `factory.update(claudeMdContent)` is called in `runTurn()` before each turn, mutating the shared reference in place. This preserves the per-turn mutation pattern without either consumer needing re-injection.

**`container.ts`**: The composition root function. Takes `{ configLoader, modelOverride }`. Returns `IServiceProvider`.

## ApprovalNotifier constructor change

`ApprovalNotifier` previously took `ApprovalNotifyConfig | null`. Changed to `ConfigLoader` with lazy read at `start()` time. Reason: `configLoader.config` is available at build time (pre-loaded), but lazy access is the correct pattern regardless — the notifier should read live config, not a snapshot. Five tests in `ApprovalNotifier.spec.ts` change their construction call; behaviours identical.

## Stop note: `ReadLine` ordering

`ReadLine`'s constructor sets `process.stdin.setRawMode(true)`. If the container resolves `ReadLine` eagerly at `buildProvider()`, stdin enters raw mode before `renderer.enter()`. Current code constructs `ReadLine` after `renderer.enter()`. Plan flags this as Q4 — if ordering causes visible artifacts, `ReadLine` stays outside the container.

## Nothing invented

All decisions with genuine forks are surfaced as open questions (Q1–Q6), not resolved by guesswork. The plan is complete and implementable as written.


# 05:33

Maker cast for the core-di-lite introduction. Phase 2 complete. All changes staged, not committed.

## What landed

**New files (6)**:
- `src/setup/SdkChannel.ts` — named `ControlChannel<SdkMessage>` subclass for DI identity
- `src/setup/ConsumerChannel.ts` — named `ControlChannel<ConsumerMessage>` subclass for DI identity
- `src/setup/ModelOverrides.ts` — replaces the three-field `overrides` plain object and five inline cycle functions in `main()`. Implements `ModelSettings`.
- `src/setup/AppToolsService.ts` — wraps `createAppTools()` so `tools`, `store`, `refTransform` are one injectable singleton
- `src/setup/DurableConfigFactory.ts` — replaces `mapConfig()` closure. Owns the shared mutable `DurableConfig` reference. Imports `logger` from `../logger.js` (module singleton; plan's code block omitted this import — added it)
- `src/setup/container.ts` — composition root. All ~35 `new X(...)` constructions moved here.

**Modified files**:
- `apps/claude-sdk-cli/package.json` — added `@shellicar/core-di-lite: ^1.0.2` dependency; `pnpm install` ran
- `src/model/ApprovalNotifier.ts` — constructor now takes `ConfigLoader<any>` instead of `ApprovalNotifyConfig | null`; reads `this.#configLoader.config.hooks.approvalNotify` lazily
- `src/entry/main.ts` — all construction removed; `buildContainer()` called after `configLoader.load()`; activation code uses `provider.resolve(...)` throughout
- `test/ApprovalNotifier.spec.ts` — added `fakeConfigLoader()` helper, replaced all construction args
- `test/AgentMessageHandler.spec.ts` — fixed `new ApprovalNotifier(null, ...)` with inline fake config loader (plan §5 said "Unaffected" but constructor change rippled here)

## Deviations from plan

1. **`ConfigLoader<T>` generic arg**: Plan wrote `ConfigLoader` bare in type annotations. `ConfigLoader<T extends z.ZodType>` requires a type arg. Used `ConfigLoader<any>` throughout — consistent with Q1's note that config is effectively `any` due to Zod 4 widening.

2. **`import type { ConfigLoader }` in `container.ts`**: Plan used `import type` but `services.register(ConfigLoader)` and `x.resolve(ConfigLoader)` require it as a value. Changed to value import.

3. **Setup class imports in `main.ts`**: Plan showed `import type` for setup classes but `provider.resolve(SdkChannel)` etc. require them as values. Changed to value imports.

4. **Missing SDK imports in `main.ts`**: Plan's diff only added `QueryRunner` from `@shellicar/claude-sdk`. Activation code also needs `AnthropicAuth`, `AnthropicClient`, `ApprovalCoordinator`, `Conversation`, `SdkMessage`, `StreamProcessor`.

5. **Missing `logger` import in `DurableConfigFactory.ts`**: Plan's code block omitted it. Added.

6. **`AgentMessageHandler.spec.ts` impacted**: Plan §5 said "Unaffected" but `new ApprovalNotifier(null, ...)` broke. Fixed inline.

7. **`conversation` and `conversationState` as locals in `main()`**: Plan §6 mentioned these become `provider.resolve(...)` calls but the diff didn't show explicit local assignments. Added them before the historyReplay block.

8. **Duplicate `Conversation` import in plan's `container.ts`**: Consolidated.

9. **Unused `ITypeScriptService` import in `AppToolsService`**: Dropped.

## Gates

`pnpm run type-check`: PASS (0 errors)
`pnpm run test`: PASS (674 tests / 41 files)
`pnpm run build`: PASS

## Q4 outcome

The `ReadLine` raw-mode risk: resolved `ReadLine` before `renderer.enter()`, matching the plan's diff exactly. If stdin raw mode before alt-buffer causes visible artifacts, this is the place to look.

# 19:09

Cleaner cast for the core-di-lite introduction.

## What biome fixed

`ci:fix` automatically formatted 8 of the 9 source files the Maker touched (excluding `package.json` and `pnpm-lock.yaml`, which biome doesn't check). One additional manual fix was required in `DurableConfigFactory.ts`: two `useBlockStatements` violations that biome classified as unsafe fixes and therefore skipped. Fixed by wrapping the single-line `return` statements in `getEffectiveThinkingEnabled()` with braces.

## Files touched by biome (automatic)

- `src/entry/main.ts` — import ordering and formatting
- `src/model/ApprovalNotifier.ts` — formatting
- `src/setup/AppToolsService.ts` — import ordering
- `src/setup/ConsumerChannel.ts` — formatting
- `src/setup/container.ts` — import ordering and formatting (largest change: 136 lines net)
- `test/AgentMessageHandler.spec.ts` — formatting
- `test/ApprovalNotifier.spec.ts` — formatting

## Files touched manually

- `src/setup/DurableConfigFactory.ts` — `useBlockStatements` for two `if (t === ...) return` guards

## Gates

`pnpm run ci:fix`: PASS (no errors after manual fix)
`pnpm run ci`: PASS
`pnpm run type-check`: PASS
`pnpm run test`: PASS
`pnpm run build`: PASS
# 17:58

Courier — configurable tool permissions (#101). Shipped.

## What was built

The hard-coded `permissions` const in `permissions.ts` is gone. `getPermission` now
takes the permission matrix as a required 4th parameter (`matrix: PermissionConfig`)
and `IFileSystem` as a required 5th (`fs: IFileSystem`). The config schema has a
`permissions` block (two zones: `default` for inside cwd, `outside`; each with
`read`/`write`/`delete` as strings `approve`/`ask`/`deny`).

Live wiring in `main.ts`: `getPermissionMatrix` reads `configLoader.config.permissions`
at call time, maps config strings to `PermissionAction` via `actionFromConfig`, and
returns a `PermissionConfig`. `AgentMessageHandler` stores `getMatrix: () => PermissionConfig`
and calls `this.#getMatrix()` at approval time. Same pattern as `getEffectiveModel`.

When permissions config changes, `conversationState.spliceNotice(formatPermissionsDisplay(config.permissions))`
injects a `🔔 notice` block immediately — mid-turn safe, not deferred to idle.

## Path normalisation: the gate must match the door

Every file tool calls `expandPath(input.path, fs)` before acting. `isInsideCwd` must
see the same expanded path, or `~/...` paths resolve under cwd and get the `default`
zone instead of `outside`. Fix: `getPermission` calls `expandPath(rawPath, fs)` before
`isInsideCwd`. `IFileSystem` is threaded as the 5th param and recursed through Pipe.

## Schema: per-zone defaults matter

`zonePermissionsSchema` was split into `defaultZonePermissionsSchema` (approve/approve/ask)
and `outsideZonePermissionsSchema` (approve/ask/deny). Every field's `.default()` and
`.catch()` equals that field's real value for its zone. Previously all-deny field defaults
meant a partial config (e.g. only `read` specified) silently denied the omitted fields.
The single `deny` remaining in the schema is `outside.delete`, which is its real value.

## Notice: `notice` block type, not `meta`

`meta` maps to the `query` label in `renderConversation.ts`. Injecting as `meta` groups
the notice visually with the turn's query_summary block. Added `'notice'` to `BlockType`,
rendered as `🔔 notice` (`BLOCK_EMOJI`). Use this type for any injected status notices.

## Startup spurious notices

`ConfigLoader` already deduplicates via JSON.stringify equality + 100ms debounce.
`onChange` only fires when the config genuinely changes. No permission-comparison logic
is needed inside the onChange callback — `formatPermissionsDisplay` is always called,
which is fine.

## `schema/sdk-config.schema.json`

A generated JSON-schema mirror of the Zod config schema. Regenerated by
`pnpm --filter @shellicar/claude-sdk-cli run build`. Must be staged alongside every
`schema.ts` change. Forgetting this leaves the mirror advertising the old defaults.

## Required options on AgentMessageHandler

`getMatrix` and `fs` are required options. Changing required options on this class
always breaks the `makeOpts` test factory. Pattern across every phase: add the new
option to `makeOpts` in `AgentMessageHandler.spec.ts` with a test stub.

## Post-merge: pnpm install may be needed

After the Phase 3 merge (integrating origin/main), the lockfile merged cleanly but
`node_modules` was stale. `@js-joda/core` was added in #338 and not yet installed
locally. Symptom: `Cannot find package '@js-joda/core'` in 5 test suites. Fix: `pnpm install`.
If you see this after a fresh checkout of this branch, run `pnpm install` first.

## Staged testament blocks git merge

A staged-but-uncommitted file blocks `git merge` if the incoming side would update or
delete it. If another merge may follow, either commit the testament or leave it
untracked. Don't leave it staged between phases.

## String-to-enum mapping

Config stores `approve`/`ask`/`deny` (strings). `PermissionAction` is a numeric TypeScript
enum. Mapping lives in `main.ts`'s `actionFromConfig`. Adding new zones or actions
requires updating both `actionFromConfig` and the Zod schema.

# 19:47

## Phase 4 (Maker, Opus): merged origin/main into the refactor branch

Merged `origin/main` (7 commits beyond merge-base) into `feature/introduce-core-di-lite`.
Main's commits were written against the OLD pre-refactor `main.ts`; the job was to
re-express their behaviour through the DI composition root, not to revert the refactor.

Git produced two real conflicts (`package.json`, `entry/main.ts`). Everything else
auto-merged, including `AgentMessageHandler.spec.ts` (the brief expected it to conflict;
it did not). But two build-breaks were hidden behind the clean auto-merge:
- `systemPrompts.ts` was deleted on main; `DurableConfigFactory.ts` (new on our branch)
  still imported it.
- main added required `getMatrix`/`fs` options to `AgentMessageHandler`; the container's
  construction didn't pass them.
Lesson: after a merge, a clean `git status` is not enough — grep for references to
deleted files and check that new required constructor params reach the composition root.

### Where main's features were re-homed
- **System prompts** (`SystemPromptLoader` + `composeSystemPrompts`, async SYSTEM.md read):
  loader registered in `container.ts`; resolution mechanism moved into
  `DurableConfigFactory` as `resolveSystemPromptsFor(sessionId)` / `needsSystemPromptResolve(sessionId)`.
  Async I/O = activation: `main()` calls it once at startup and again on session change in
  `runTurn`; `factory.update()` (already per-turn) folds the result into `config.systemPrompts`.
  The `--system` flag text is threaded as `systemFlagText` through `ContainerOptions`.
- **Permission matrix**: main's module-level `actionFromConfig` (string->enum) is now
  `buildPermissionMatrix` in `permissions.ts` (the permission domain, i.e. logic, not the
  container). Container's `AgentMessageHandler` registration calls it live:
  `getMatrix: () => buildPermissionMatrix(configLoader.config.permissions)`. This supersedes
  the earlier testament note that `actionFromConfig` lives in `main.ts` — it no longer does.
- **Flasher** (disposable): registered in `container.ts`, resolved as `using _flasher` in `main()`.
- **`--config` override, `NodeDirectoryWatcher`, `spliceNotice` in onChange, configOverride
  meta block, `markPromptStart`, new StreamProcessor events**: kept in `main()` (they are
  activation / turn-loop / config wiring, which legitimately live there).

### One structural move
main's `onChange` now calls `conversationState.spliceNotice(...)`, but the refactor resolved
`conversationState` ~180 lines below the onChange registration. Moved
`const conversationState = provider.resolve(ConversationState)` up next to `statusState`
(removed the later duplicate). All container resolves are side-effect-free, so resolving
earlier is free.

### Gates and the no-Cleaner gap
type-check, test, build all pass. `pnpm install` was needed first — the merge added
`@js-joda/core` to package.json but node_modules wasn't materialised (errors looked like
missing types + cascading `override` errors on FakeClock).

The phase order is Apostle->Maker->Cleaner->Maker(merge)->Courier: there is NO Cleaner
after this merge. `biome ci` flagged exactly one self-introduced issue (import order in
`DurableConfigFactory.ts`); I fixed that one line by hand (not a broad `ci:fix`, which could
disturb auto-merged files). `pnpm run ci` is now clean across all 463 files. If a later
merge re-touches these files, watch for biome import-order again with no Cleaner to catch it.
