# Ghost-text: the wrap-boundary render defect, fixed by a cell-grid renderer

Consolidated arc for the `fix/ghost-text` PR (Investigator → Maker → Courier). This is the
ghost-text rendering work; the SQLite/SEA legacy in `2026-06-22.md` is a different arc and stays
there.

## The bug

Content near the right margin was duplicated: a stale fragment from the previous frame survived at
the wrap boundary. First seen as input echoed into the response region one keypress behind, later on
streaming response/tool output too, so it is **not** input-specific.

`TerminalRenderer.paint` wrote each row as `\r clearLine row \n` and trusted the bare `\n` to advance
exactly one physical line. A response line that fills the terminal width (a long unbroken token that
`wrapLine` emits as an exactly-`cols`-wide line), written into an autowrap-on terminal, can consume
an extra physical line; the `\n` then over-advances and strands a line still holding the prior
frame's content. The bet that `\n` advances one physical line is terminal-dependent, which is why the
ghost was intermittent and why "less often now" tracks `string-width` improving.

**Trap for anyone reading the old investigation notes:** the trigger is a wrapped **response** line,
not the input/editor line. The editor line is short and never wraps (`wrapLine` early-returns short
lines, `reflow.ts`). An early investigation iteration pinned it on the editor line and was wrong.

## What shipped

A cell-grid renderer where the ghost cannot occur by construction:

- **`apps/claude-sdk-cli/src/view/ScreenBuffer.ts`** — the screen as a cell grid. `layoutRow`
  (styled row → cells, ANSI carried as a zero-width prefix onto the next glyph, wide graphemes get a
  continuation cell, clip at margin), `buildGrid`, `diffToWrites`, `gridToLines`. Pure functions.
- **`src/view/TerminalRenderer.ts`** — rewired onto the grid: holds the previous grid, builds the
  next, writes only changed rows at absolute `cursorAt` positions with **autowrap off**. Resets the
  buffer to null on resize, alt-buffer enter, and inline write-through. No reliance on cursor advance
  or margin wrap, so the ghost class is gone.
- **`packages/claude-core/src/ansi.ts`** — `disableAutowrap`/`enableAutowrap` (`?7l`/`?7h`).
- **Test infra:** `test/VirtualScreen.ts` — a terminal-grid emulator implementing the `Screen`
  interface, so a real `TerminalRenderer` runs against it unmocked. Models only the ANSI `paint`
  emits plus last-column autowrap, with the `?7` toggle honoured. Built because `paint` had **no**
  test and there was no virtual screen in the codebase (the brief's premise that one existed was
  false; the Maker surfaced it and the SC approved building it as scope).
- **Tests:** `ScreenBuffer.spec.ts`, `VirtualScreen.spec.ts`, `TerminalRenderer.spec.ts`. 24 green,
  type-check clean.

Two commits on the branch: `0f55167` (Tier 1b — absolute-addressing the `\n`-based paint) then
`c39167e` (Tier 3 — the cell grid, superseding Tier 1b's paint). Same branch, squash-merges.

## Two non-obvious truths (do not relearn the hard way)

1. **`diffToWrites` MUST be row-granular, not cell-granular.** ANSI colour is cumulative across a
   row — a cell's colour is set by an escape in an earlier cell. A cell-granular diff that writes
   only the changed cell drops that SGR state, so a partial update renders in the wrong colour (this
   was a real command-mode styling glitch in the first Tier-3 cut). The fix: rewrite the whole row
   from column 1 whenever it changes. A test proves it — revert to cell-granular and it goes red
   (`\x1b[1;2HC\x1b[0m`, no colour prefix) vs green (`\x1b[1;1H\x1b[31mAC\x1b[0m`). Do not "optimise"
   back to cell-granular without first solving SGR-state carry.

2. **Copy-as-newlines is architectural, not a bug.** Because the app wraps lines itself and writes
   each wrapped row as its own grid line, the terminal/tmux see separate lines and copy inserts a
   newline at every wrap point. Preserving a soft-wrap on copy means letting the *terminal* wrap
   (autowrap on, full logical lines written) — which reintroduces the ghost class. It is the cost of
   owning the layout; the old `\n`-based paint had it too.

## Build / tooling traps

- The worktree has no `dist/` for workspace packages, which resolve via `exports` to `dist/esm`.
  `pnpm --filter @shellicar/claude-core build` (plus claude-sdk, claude-sdk-tools) before tests or
  type-check resolve. After editing `ansi.ts`, rebuild claude-core or the new constants aren't seen.
- `TsDiagnostics` uses its own tsconfig and complains about `#private` and `Intl.Segmenter`; trust a
  direct `pnpm type-check`.
- Local `pnpm type-check` and vitest do **not** run Biome. `pnpm run ci` (`biome ci`) is a separate
  gate, and import-sort errors only surface there. Run it before pushing — two of these slipped to
  CI on this PR. Fix with `pnpm ci:fix`, never `biome check --write` directly.

## Decisions and an open thread

- **The SC drove the scope, not the brief.** The mission was scoped as a characterization test only
  ("no production change, stage don't commit"). In conversation the SC took it to a Tier 1b fix, then
  a full Tier 3 rewrite, and committed. Those crossings were the SC's direct calls.
- **The emulator confirms the mechanism conditionally.** A self-authored virtual screen proves the
  ghost under *immediate* wrap and not under documented *deferred* wrap. The SC's terminal (iTerm2)
  is the relevant case and Test 1 makes the emulator's behaviour explicit, but a width disagreement
  (`string-width` thinks a line is `cols` while the terminal renders a glyph wider — emoji / CJK /
  ZWJ) is the strongest candidate for a real wrap on a deferred terminal. Left as a lead.
- **Open thread:** the command-mode colour fix proves a real diff-correctness bug in the new
  renderer's own first cut. Whether it is the same "command-mode weirdness" the SC saw on the older
  running build is unconfirmed — the link was surfaced, not oversold. Worth a recheck against a
  rebuild.

## Related

The layered presentation architecture is written down: `tui-architecture.md` in the tower repo
(`23c09dd`). The Tier 3 cell grid is its first real instantiation.
# exec-core ship: the traps and the changelog tooling

The exec-core arc lives in `2026-06-25.md` (the leaf, the two findings, the flush
race, the tsconfig consolidation). Read it first. This entry is the durable guidance
that came out of shipping it.

## The temporal-fix rule (most likely to bite you)

The whole exec-core PR is unreleased and squash-merges to main as one change. So the
baseline for any changelog entry or PR bullet is **released main**, never the branch's
own history. A bug the consolidation introduced and then repaired on the same branch
is net-zero against main: no consumer ever saw it, so it is not a "fix" to document.

Three of these were deliberately kept OUT of both the changelog and the PR body:

- the already-aborted abort guard (released main passed the signal to `spawn`, which
  refuses an already-aborted one, so main already stopped chained commands after a
  cancel; the consolidation dropped that and the guard restored it),
- bad cwd staying exit 126 in a pipeline (was 127 on the branch only),
- the sink flush race (introduced by exec-core's own sink handling).

The reflex to write "now fixes X" is strong and wrong here. Ask: did released main have
X? If not, it is temporal. Leave it out. The SC caught two of these; do not make him
catch a third.

## Changelog tooling (new, and now enforced)

`changes.jsonl` and `CHANGELOG.md` must never drift, and two things now hold that:

- `pnpm --filter scripts run changelog` regenerates every package's CHANGELOG from its
  changes.jsonl in one shot. It wraps the unchanged `generate-changelog.ts`.
- CI (`node.js.yml`) regenerates and runs `git diff --exit-code` on the changelogs, so
  any drift reds the build.

Workflow: edit a changes.jsonl, run the script, commit both together. A hand
regeneration earlier missed `mcp-exec` entirely; the all-script exists so you never
have to remember the full package list.

## Vocabulary

"operator" is fleet terminology. It has no home in anything a consumer reads (PR bodies,
changelogs, docs). The exec-core code comments say "operator's tty" internally; do not
carry that outward. The terminal belongs to whoever runs the CLI.

## Shared worktree

This worktree is shared with co-working sessions. Mid-cast, another session rebased the
branch onto a freshly-advanced main and staged/unstaged files under me. Before any
commit or push, run `git status` and resolve refs explicitly (`git rev-parse`, reflog)
rather than trusting the last SHA you saw. The reflog is the authoritative record of who
moved HEAD.
# 02:55 — Scaffolder, messages-api-layer Phase 3 (recast: compaction accumulation)
# Owned HTTP/SSE transport — what shipped and what to know

This PR replaces the `@anthropic-ai/sdk` *runtime* in `@shellicar/claude-sdk` with our own transport. The SDK stays a dependency for its **types** only (`BetaMessageParam`, `BetaRawMessageStreamEvent`, and friends). The reason we own it: the SDK honoured a `retry-after` of 24h+ with an uncapped, unabortable wait, freezing the CLI. Owning the request, the SSE stream, and the error/retry path is the only way to take that control.

## What was built

New, in `packages/claude-sdk/src/private/http/`:

- `transport.ts` — owned `fetch` + SSE request (`streamMessages`). Non-2xx → `HttpError` carrying status + parsed `retryAfterMs`; fetch reject → `ConnectionError`; caller-aborted signal → throws the abort reason.
- `sse.ts` — wire parsing: `findFrameBoundary`, `parseFrame`, `parseSse`. An `error` frame throws `ApiStreamError` (with `.type`). `ping`/comments skipped. These three are exported and unit-tested directly and exhaustively — the SC's standard: this is foundational API-communication code, it must work perfectly, so it is pinned at the unit level, not only through `parseSse`.
- `accumulator.ts` — `MessageAccumulator` assembles raw deltas into content blocks (text/thinking/signature concatenate; tool input JSON buffered and parsed once at block stop; `compaction_delta` concatenates onto the compaction block).
- `errors.ts` — owned error classes + `parseRetryAfter` (`retry-after-ms` → ms, `retry-after` seconds → ×1000, HTTP-date → forward delta).

Rewritten: `AnthropicClient` (owned transport, SDK subclass + event emitter gone), `MessageStreamer` (returns the owned async-iterable), `StreamProcessor` (consumes raw events, assembles in-process, emits `final_message`), `backoff` (owned-error `isRetryable`/`isAccountLimit`), `TurnRunner` (the retry/give-up loop), `QueryRunner` (give-up catch). Deleted: `TokenRefreshingAnthropic.ts`, `sdkInternals.ts`, and the `sdkInternals` biome override.

CLI: `AccountLimitNotice` (the `⏳`/`🛑` notice), audit subscription moved to `final_message` in `main.ts`, notice wired in `container.ts`.

## How the boundary works (Option B from the plan)

`IMessageStreamer.stream` returns `IMessageStream = AsyncIterable<BetaRawMessageStreamEvent>`. `StreamProcessor.process` consumes raw events and assembles via `MessageAccumulator`, so nothing downstream (control channel, consumer) ever sees a stream object — only `StreamProcessor` changed shape. Handy fact: the SDK's own `BetaMessageStream` *is* an `AsyncIterable<BetaRawMessageStreamEvent>`, so existing fakes that return it still satisfy `IMessageStream`.

## The v1 retry behaviour

- Every honoured `retry-after` is capped at `RETRY_AFTER_CAP_MS` (60s). The SDK's 24h+ honouring is deliberately dropped.
- A 429 whose `retry-after` exceeds the cap is the non-transient **account-limit** case. It retries (capped) up to `ACCOUNT_LIMIT_BUDGET_MS` (10 min), surfacing a notice each retry, then gives up with `AccountLimitStoppedError`. `QueryRunner` catches that and publishes no `error` — the user sees a single `🛑`, not a raw stack.
- Transient errors (connection/timeout/408/409/429-within-cap/5xx) retry on bounded exponential backoff (`MAX_RETRIES`), **silently** by design.
- ESC aborts the wait at any point and throws the abort reason.

## SC corrections worth carrying

- **The account-limit notice is not de-duplicated.** An earlier brief carried a fabricated requirement ("one persistent notice, refreshed, not a new line each minute") that the SC never gave. It became a `#active` gate that made the lockout nearly invisible — one line. Removed: every 429 raises the notice, shown on every retry. Do not reintroduce a gate.
- **`cleared()` is gone, not stubbed.** It existed only to reset that gate. With the gate removed it was unanchored dead code; an empty method with a comment justifying its emptiness is the tell. Removed end to end (the listener type, the `TurnRunner` ESC call, the notice method, the tests) rather than left as a no-op.

## Traps the next operator will hit

- **`pnpm run type-check` / `pnpm run test` abort under no-TTY** with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` — pnpm's verify-deps gate wants to purge `node_modules` and can't prompt. Drive the toolchain directly instead: per package, `node_modules/.bin/tsc -p tsconfig.check.json` and `node_modules/.bin/vitest run`. The CLI type-check needs the workspace deps' `dist/` present, so `tsup`-build `claude-core`, `claude-sdk`, `claude-sdk-tools` first. The root `tsconfig.check.json` extends a non-existent root `tsconfig.json` — run the per-package one, never the root standalone.
- **SDK 0.92.0 `.d.ts` shape:** `context_management` sits on the `message_delta` *event*, not on its `delta`. Delta usage fields are nullable, so cumulative usage must coalesce per-field, not spread wholesale.
- **`StreamProcessor.mapBlocks` guards a sparse-content hole** (`if (block == null) continue;`). Production streams are contiguous-from-0, but a server-tool-result test opens a block at index 1 with no index 0; the assembly must not crash on the hole.
- **Manual API testing is required before merge.** This rewrites how requests are issued and streamed; build and type-check don't cover the wire behaviour. The SC manually drove the account-limit path (a `SIMULATE_429` block in `transport.ts` plus shrunk caps in `backoff.ts`) and confirmed `⏳`-per-retry then `🛑`. That scaffolding was stripped before the feature commits — if you reintroduce it to test, strip it again before committing.

## A follow-up the SC raised but did not request

Transient retries are silent today. The SC floated surfacing them with their own line too (same "don't hide behaviour" principle) but did not ask for it — it's a deliberate requirement change, not part of this work.
