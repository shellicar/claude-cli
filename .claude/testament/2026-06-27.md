# 19:24

## repaint-every-cell — Phase 1 Maker (iteration 1)

Killed the orphaned-character ghosting by replacing the row-diff in
`ScreenBuffer.diffToWrites` with a full repaint: every row, every frame, from
column 1. The previous-grid skip was the bug — under tmux the grid is reflowed
behind the renderer's back (resize, copy-mode entry), so a row our model
believed unchanged could still hold orphaned cells, and the skip kept them
forever. `_prev` is retained only for call-site compatibility; `TerminalRenderer`
still uses `#prevCols/#prevRows` for size-change detection, untouched.

### What landed
- `ScreenBuffer.ts`: full-repaint `diffToWrites` + both doc comments rewritten.
- `ScreenBuffer.spec.ts`: the identical-grids case flipped from "emits nothing"
  to "repaints the row" (`\x1b[1;1Hsame  `).
- `repaintEveryCell.spec.ts`: the new proving test. Red before the fix (`Z`
  injected via `poke` survives the skipped row), green after. Confirmed the red
  was for the right reason.

### The one decision worth knowing
The plan's verbatim test source does **not** pass the repo's Biome gate, even
though the mission requires `ci:fix` clean. Two clashes:
1. `noNonNullAssertion` — the plan used `this.#grid[row]![col]`. The app tsconfig
   has `noUncheckedIndexedAccess` off, so `string[][]` indexing already yields
   `string[]`; the `!` is both forbidden and unnecessary. Dropped it. This is the
   established convention — see `test/VirtualScreen.ts`, which indexes
   `#grid[row][col]` bare.
2. `noControlCharactersInRegex` — the plan put a `biome-ignore` on only one of
   the three control-char regexes. Added the missing two (CURSOR_RE and the
   inline `/\u001b/`), matching VirtualScreen's exact ignore comment.

Behaviour of the test is identical; only lint-conformance changed. I made this
call rather than stopping because it's convention-following inside the test, not
a reshape of the deliverable. Surfaced in the debrief.

### Build trap (still true, from prior testaments)
Workspace packages have no committed `dist/`. Tests/type-check resolve via
`exports` to `dist/esm`, so you must `pnpm -r --filter ./packages/* build` first
or you get "Cannot find package @shellicar/...". `createAppTools.spec.ts` needs
`@shellicar/exec-core` built specifically; building only claude-core/sdk/tools
leaves that one suite failing to resolve.

### Verify results
- `pnpm --filter claude-sdk-cli test`: 61 files, 928 tests green (plan estimated
  ≈969; estimate, not a discrepancy that matters — suite is fully green).
- `pnpm --filter claude-sdk-cli type-check`: clean.
- `pnpm ci:fix`: clean.
# 14:44 — Maker (Phase 2, iteration 1): Memory tool built to the plan, all gates green

Built the complete Memory feature from the Apostle's plan. Everything verifies: `pnpm turbo build type-check test` across claude-core/claude-sdk/claude-sdk-tools/claude-sdk-cli is 13/13 green, and `pnpm run ci` (biome) is clean. The acceptance test the SC actually runs — a live Claude driving the tool round-trip — is his to run; the build makes it real.

## What the next operator (the Courier) needs

- **Branch is `feature/testament-tool`, HEAD `f4a1d0c`.** The plan was written against `a242e0c`; the branch has since taken #375 (API transport), #372 (ghost text), #371, #373. None of those touched the Memory shared-surface files — every "before" in the plan matched the tree exactly. No rebase pain.
- **The pre-existing dirty `.claude` files are NOT mine.** `.claude/CLAUDE.md` (staged) and `.claude/testament/2026-06-26.md` (modified) were already in the tree when I started — fleet scaffolding in this shared worktree. I staged only my own files by explicit path. Do not sweep these into a commit.
- **Changelog still owed.** Per the mission the Courier appends the Memory `changes.jsonl` entry to each changed package (claude-core, claude-sdk-tools, claude-sdk-cli — NOT claude-sdk: it is read, not modified) and regenerates with `pnpm --filter scripts run changelog`. I did not touch changelogs.

## Two places the plan did not hold against the code, and what I did

1. **`createMemoryTools` return had to be a tuple, not an array.** The plan's `return [WriteMemory, ReadMemory, ...]` is inferred by TS as `Array<unionOfAllFive>`, so the handler tests that destructure and call a *specific* tool (`call(WriteMemory, ...)`, `result.found`) failed type-check. The plan never hit this because it was never compiled. Fix: `return [...] as [typeof WriteMemory, typeof ReadMemory, typeof SearchMemory, typeof DeleteMemory, typeof MemoryTypes]`. Realises the plan's intent; the test tier requires precise per-tool types.
2. **`src/entry/Memory.ts` could not use `export *`.** The plan's barrel used `export * from '../Memory/schema'`, which biome's `noBarrelFile`/`noReExportAll` reject at error level. The existing entries (Exec.ts etc.) use explicit named imports + re-exports — so I matched that: explicit `export { ... }` / `export type { ... }` of `createMemoryTools`, the schemas, and the types.

## Decisions I made (the plan left these to the operator)

- **`RecordingMemoryStore` is duplicated locally in the app test dir**, not imported cross-package. The app test already keeps a *local* `MemoryObjectStore.ts` (the tools-package `test/` dir is not exported), so a cross-package import would break the app tsconfig. I followed that established precedent: `apps/claude-sdk-cli/test/RecordingMemoryStore.ts` mirrors the one in `packages/claude-sdk-tools/test/`. Two copies, but consistent with how doubles already cross the package boundary here (i.e. they don't).
- **`scripts/src/tool-schema-sizes.ts` uses inline `StubObjectStore`/`StubMemoryStore`** rather than importing the test doubles. The plan flagged the cross-package `test/` reach as awkward and permitted an inline stub; the script is a standalone `tsx` utility, so self-contained stubs are cleanest. (The old `createAppTools(stubTs)` single-arg call was already type-broken before this mission; the options-object conversion fixes it.)

## Grounding that held (trust these)

- **FTS5 + bm25 + porter run green on this machine's Node** via the engine integration tests (17/17, including the full adversarial input-safety set). The `SqliteMemoryEngine` constructor fires `CREATE VIRTUAL TABLE … USING fts5` — that's the earliest-signal check, and it passed in the real test run, not just in theory.
- **`@dependsOn` works exactly as the Apostle grounded it.** The two injectables (`SqliteMemoryStore`, `GitMemoryEnvironmentProvider`) build, type-check, and the app's container resolves them — empty constructors, public `!` fields, `.to(Impl)` registration, lazy engine open. No `verbatimModuleSyntax`/`useDefineForClassFields` misbehaviour; the stop-and-report fallback was not needed.
- **tsup globs meant no config edits.** claude-core's `entry: ['src/**/*.ts']` and claude-sdk-tools' `entry: ['src/entry/*.ts']` both pick up the new files automatically. claude-core's wildcard `./*` export resolves `memory/*` with no package.json change; only claude-sdk-tools needed the explicit `./Memory` subpath added.
- **The config-default snapshot test needed updating.** `cli-config.spec.ts > returns defaults for empty object` snapshots the whole default config; adding the `memory` block legitimately changes it. Added `memory: { tenantId: null, environment: {}, git: { enabled: true } }` to the expected object. The build also regenerated `schema/sdk-config.schema.json` (+49 lines) — that's `build.ts`'s job, not hand-edited.

## biome trap (re-confirms the 2026-06-26 testament)

`pnpm type-check` and `vitest` do NOT run biome. Only `pnpm run ci` catches import-sort, formatting, barrel-file, and member-accessibility. It flagged 18 errors after a clean type-check/test. Fix flow: hand-fix the non-auto-fixable ones (barrel file, `public` modifiers), then `pnpm ci:fix` (never `biome check --write` directly), then re-run `pnpm run ci`. Run it before pushing.
