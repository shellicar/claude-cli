# Error handling

## What changed

**`main.ts`** — `uncaughtException` and `unhandledRejection` handlers added after the signal handlers. `uncaughtException` logs and returns; no exit, no cleanup, no special cases. `unhandledRejection` logs only. Two-stage SIGINT: first Ctrl-C calls `cleanup()` (which exits via `process.exit(0)` internally); second Ctrl-C calls `process.exit(1)` as an escape hatch if cleanup hangs. `process.title` set to `'claude-sdk-cli'` at module level before `parseArgs`.

**`NodeProcessLauncher.ts`** — `launch()` was `spawn(...).unref()`. Extracted to variable, added `error` listener before `unref()`. ENOENT on a fire-and-forget hook no longer crashes the CLI.

**`execPipeline.ts`** — `error` listener on `next.stdin` in both the `merge_stderr` and direct pipe paths inside the connection loop. `error` listener on `stream` in the redirect block.

**`execCommand.ts`** — same redirect pattern: `error` listener on `stream` after `createWriteStream`.

**`TsServerService.ts`** — `stdin?.on('error')` and `proc.on('error')` in `start()`, after the stdout data handler.

## Key decisions

The `uncaughtException` handler does not call `cleanup()` or exit. Calling cleanup from an unknown process state is unsafe. The handler exists to keep the process alive after an unexpected error. SIGINT owns graceful shutdown.

The SC corrected Phase 1: the original handler swallowed EPIPE and crashed on everything else, which defeated the purpose. Phase 2 simplified it to log-and-return unconditionally.

`cleanup()` calls `process.exit(0)` as its last step. With the two-stage SIGINT pattern, `sigintReceived` is set to `true` before calling `cleanup()`, so a second Ctrl-C during the (brief) cleanup window hits `process.exit(1)`. Both paths terminate the process.

# 19:37

## Phase 1 — Config loader scaffold (issue #301)

Role: Scaffolder. Branch: `feature/config-origin`.

### Files created in `packages/claude-core/src/`

- `IConfigFileReader.ts` — sync abstract for reading config files. Kept sync because the current `loadConfig` is sync and startup is one-shot.
- `IConfigWatcher.ts` — abstract + `ConfigWatchHandle` interface. Raw events only; the loader owns debounce/diff.
- `IConfigLoader.ts` — abstract `IConfigLoader<T extends z.ZodType>`, plus `ConfigSource`, `ConfigChangeListener`, `ConfigUnsubscribe` types.
- `ConfigLoader.ts` — concrete stub. Every method throws `not implemented`. Accepts `{ schema, paths, reader, watcher?, mergeOptions?, debounceMs?, logger? }`.
- `NodeConfigFileReader.ts` — `node:fs` sync reader (`existsSync` + `readFileSync`).
- `NodeConfigWatcher.ts` — `node:fs.watch` wrapper, silently ignores missing files on registration.
- `MemoryConfigFileReader.ts` — in-memory fake with `set()`/`delete()` mutators so tests can simulate file changes between `load()`/triggers.
- `MemoryConfigWatcher.ts` — fake watcher with `trigger(path)` method; exposes `watchedPaths` and `isActive` for test introspection.

### Test file created

`packages/claude-core/test/ConfigLoader.spec.ts` — 12 tests across 6 describes covering all 10 minimum cases from the mission. One extra describe (sources) has 3 tests because mission minimum 5 is split into two its (one for home-owned field, one for local-overridden field) to satisfy "one assertion per it".

### Key design decisions

1. **Sync reader, async watcher.** The reader is synchronous because startup load happens once and the existing `loadConfig` was sync. The watcher is callback-based (async-by-nature) since file watching is event-driven.

2. **Loader owns debounce and diff.** The watcher just fires raw events. `ConfigLoader` debounces (default 100ms; 0 for tests) and compares parsed configs via `JSON.stringify` to suppress no-op emissions. Keeping this in the loader means every `IConfigWatcher` implementation stays trivial.

3. **`load()` separate from `start()`.** Constructor stores options only. `load()` performs the initial read. `start()` begins watching. `dispose()` stops. This lets tests check post-load state without triggering a watcher at all, and makes reload-on-change a simple re-call of `load()` internally.

4. **Sources preserve raw JSON, not parsed config.** `ConfigSource = { path, raw }` where `raw` is the object from `JSON.parse` before the schema runs. This matches the mission's rationale: parsed content erases "explicitly set" vs "defaulted by schema". Consumers walk sources in reverse for last-writer-wins origin tracking.

5. **Logger is a minimal local type.** `ConfigLoaderLogger` has just `warn(message, meta?)`. Not reusing `ILogger` from claude-sdk because claude-core should not depend on claude-sdk.

6. **`MergeOptions.additivePaths` preserved.** The existing option is still used by the legacy `claude-cli` via `loadConfig(..., { additivePaths: ['execPermissions.approve'] })`. The new `ConfigLoader` accepts `mergeOptions` and Phase 2 must route it through to `mergeRawConfigs`.

### Verification

- `pnpm type-check` (claude-core): passes.
- `pnpm build` (repo): passes. tsup picks up 8 new entries automatically from `src/*.ts` glob.
- `pnpm test` (claude-core): 12 new tests fail with `Error: not implemented`; 5 existing `reflow.spec.ts` tests still pass.
- `pnpm test` (claude-sdk-cli): 473/473 still pass.

### Test contract for Phase 2

The class MUST:
- Construct without side effects; all I/O happens in `load()` or `start()`.
- `load()` reads every path in order via `reader.exists()` + `reader.read()`; missing files contribute nothing (no warning). Invalid JSON contributes `{}` and a warning containing the path.
- Merge uses `mergeRawConfigs(home, local, mergeOptions)` reduced left-to-right.
- `sources` is only populated from files that were present and parsed. Tests currently assume sources array order matches `paths` order, one entry per existing file.
- `config` is the `schema.parse(merged)` result.
- `onChange` adds listener; returns an unsubscribe.
- `start()` calls `watcher.watch(paths, onChange)` and stores the handle.
- On watcher callback: debounce (if set), re-run `load()`, compare `JSON.stringify(prevConfig) !== JSON.stringify(newConfig)`; if different, fire listeners with new config.
- Reload that throws from `JSON.parse` keeps the previous config AND the previous sources (so a transient broken edit doesn't clear origin tracking).
- `dispose()` calls `handle.dispose()` and clears the debounce timer.

### Things Phase 2 might discover

- The existing `loadConfig` silently swallows parse errors and records a warning; need to match. The new reader throws ENOENT on missing — load loop must `reader.exists()` first, not try/catch.
- The `customFetch` logger integration pattern in claude-sdk uses `ILogger` with `.warn()`. The CLI's winston logger has `.warn(message, meta?)`, which is a superset of `ConfigLoaderLogger`. Pass `logger` directly in `main.ts` wiring.
- The current `SdkConfigWatcher` logs with `logger.warn('config reload failed, keeping previous config', { error: String(err) })`. The new loader should log similarly from within its reload path, using the injected logger.
