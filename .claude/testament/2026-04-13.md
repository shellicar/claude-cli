# 23:14

## Phase 1 complete: Red tests and stubs for audit persistence

Branch: `feature/audit-persistence`. Local HEAD at `c70b231` (same as `origin/main`). Five files staged, no commit yet.

### What was done

1. Added `appendFile(path: string, content: string): Promise<void>` to `IFileSystem` abstract class
2. Added stub implementations in both `NodeFileSystem` and `MemoryFileSystem` that throw `new Error('not implemented')`
3. Created `apps/claude-sdk-cli/src/AuditWriter.ts` with an empty `write()` body (stub)
4. Created `apps/claude-sdk-cli/test/AuditWriter.spec.ts` with 4 tests using `MemoryFileSystem`

### Test results

- **4 new tests fail** (all in `AuditWriter.spec.ts`)
- **434 existing tests pass** (19 other files)
- **Type-check passes**

The failures are because `write()` is a no-op stub. The tests assert file creation, JSONL content, same-ID accumulation, and different-ID separation.

### BetaMessage import path

`BetaMessage` is imported from `@anthropic-ai/sdk/resources/beta/messages/messages.js`. This is the deep path where the interface lives. The existing test convention (see `replayHistory.spec.ts`) imports Beta types from `@anthropic-ai/sdk/resources/beta.js`, but `BetaMessage` is not re-exported there in v0.82.0. The deep path works. Phase 2 will also need to add a re-export from `@shellicar/claude-sdk` index.

### Test helper: makeMessage

Creates a minimal `BetaMessage` with `as BetaMessage` cast. Required fields: `id`, `container`, `content`, `context_management`, `model`, `role`, `stop_details`, `stop_reason`, `stop_sequence`, `type`, `usage`. The cast is needed because `BetaUsage` has additional optional fields in newer SDK versions.

### Previous cast's work

The remote `origin/feature/audit-persistence` has 3 commits from a previous cast that skipped tests and used `appendFileSync` from `node:fs` directly. Our local branch is behind by those 3 commits (intentionally). The Supreme Commander reset to `origin/main` locally.

### Suggested commit message

```
Add AuditWriter stub and failing tests for audit persistence
```
