# 01:57

Scaffolder phase for rate-limit retry mission.

## What I landed

**`backoff.ts` stub** (`packages/claude-sdk/src/private/backoff.ts`):
- Constants correct: `BASE_DELAY_MS = 500`, `MAX_DELAY_MS = 32_000`, `MAX_RETRIES = 10`
- `calculateBackoffDelay` stub returns 0
- `isRetryable` stub returns false
- `defaultSleep` stub has no abort-awareness (plain `setTimeout`)
- SDK class imports included for the Builder to fill in

**`TurnRunner.ts` constructor update** (`packages/claude-sdk/src/private/TurnRunner.ts`):
- Added `readonly #sleep` and `readonly #random` fields
- Added `sleep?` and `random?` optional constructor params with defaults (`defaultSleep`, `Math.random`)
- Import from `./backoff` added
- Stream+process pair in `run()` is untouched — no retry loop yet

**`backoff.spec.ts`** (`packages/claude-sdk/test/backoff.spec.ts`):
- Full test suite: `calculateBackoffDelay` (7 tests), `isRetryable` (8 tests), `defaultSleep` abort-awareness (2 tests)
- All delay and retryable tests fail in Red (stub returns wrong values)
- Both abort timing tests fail in Red (stub waits out full delay)

**`TurnRunner.spec.ts`** updated:
- Added `APIError`, `APIUserAbortError` imports and `MAX_RETRIES`
- `FakeMessageStreamer` extended to handle `FakeResponse = BetaRawMessageStreamEvent[] | Error`
- `FakeSleep` class (records ms, optional `onCall` hook), `makeApiError` function
- New `describe('TurnRunner — transient retry', ...)` block with 9 tests

## What the code doesn't say

**`makeApiError` constructor cast**: `APIError`'s fifth param is `ErrorType | null | undefined` (a literal union, not `string`). `new Headers()` works for the headers param (no cast). `type as any` is needed for the type param — it's test-only code constructing a third-party type where any string type-string is needed. Biome-ignore comment added.

**Red state analysis** (matches Apostle plan):
- `calculateBackoffDelay`: all fail (stub returns 0)
- `isRetryable` retryable cases: fail (stub returns false). `isRetryable` false cases: pass (accidentally correct)
- `defaultSleep` timing: both fail (stub waits full delay)
- Loop count/sleep tests: fail (no retry loop)
- `surfaces original error unchanged after exhaustion`: passes accidentally in Red (no retry, error propagates unchanged — same object)
- `does not sleep before surfacing non-retryable`, `does not retry APIUserAbortError`: pass in Red (no loop means no sleep)
- `makes no further stream() call when abort fires`: passes accidentally in Red (no loop, only 1 call)
- `surfaces abort signal reason`: fails in Red — key discriminating test. Stub throws stale `rate_limit_error`; test expects `abortReason`. Builder's `throwIfAborted()` fixes this.
- `stops retrying on success` and `retries overloaded_error`: fail in Red (error propagates immediately)

**Test result**: 19 failures (all expected Red), 162 passes (all existing tests pass).

## For the Builder

The Apostle plan's Hunk 3 abort comment contains a false claim about `throwIfAborted()` mirroring the SDK's internal abort mechanism. Phase 4 instructions already call this out explicitly — do not copy the comment, use the corrected version they specify.

`throwIfAborted()` is available in Node 24. No fallback needed.

# 02:13

Scaffolder iteration 2 — ErrorType correction and ci:fix pass.

## What changed

**`makeApiError` in both spec files**: replaced `type: string` + `type as any` with `type: ErrorType` imported from `'@anthropic-ai/sdk/resources/shared.js'`. `ErrorType` is a literal string union exported from that subpath but not re-exported from the main `@anthropic-ai/sdk` index. All test strings (`rate_limit_error`, `overloaded_error`, `invalid_request_error`) are in the union.

**Unused imports removed**:
- `backoff.ts`: removed the SDK class imports entirely. The stub bodies don't use them — the Builder adds them when implementing.
- `TurnRunner.ts`: narrowed from `{ calculateBackoffDelay, defaultSleep, isRetryable, MAX_RETRIES }` to just `{ defaultSleep }` — the only one the constructor actually uses. Builder adds the rest when adding the retry loop.

**`FakeMessageStreamer.stream()`**: added braces to `if (next instanceof Error) throw next` — biome's `useBlockStatements` rule.

**`ci:fix` result**: clean pass, no fixes applied. Zero errors.

## Red state confirmed

Same 19 failures, 162 passes as iteration 1. Pattern unchanged.

# 03:07

Builder phase for rate-limit retry mission.

## What I landed

**`backoff.ts`** (`packages/claude-sdk/src/private/backoff.ts`):
- Added import: `APIConnectionError`, `APIError`, `APIUserAbortError` from `@anthropic-ai/sdk`
- `calculateBackoffDelay(attempt, random)`: `base = min(500 * 2^(attempt-1), 32_000)`; `delay = base + random() * 0.5 * base`
- `isRetryable(error)`: abort-first branch order (never retry); then `APIConnectionError` (retry, covers timeout subclass); then `APIError` with type `rate_limit_error`/`overloaded_error` (retry); else false
- `defaultSleep(ms, signal)`: early-return on `signal.aborted`; `setTimeout` + `abort` event listener with `{ once: true }` to clear the timer on abort

**`TurnRunner.ts`** (`packages/claude-sdk/src/private/TurnRunner.ts`):
- Import expanded from `{ defaultSleep }` to `{ calculateBackoffDelay, defaultSleep, isRetryable, MAX_RETRIES }`
- Two-line stream+process replaced with retry loop: attempts 1 to MAX_RETRIES+1; non-retryable or exhausted → throw; retryable → sleep, then explicit `if (abortSignal.aborted) throwIfAborted()`
- `let result!: MessageStreamResult` — definite assignment assertion, safe: loop exits only via `break` (assigns) or `throw`

## Abort comment — the correction carried forward

The plan's Hunk 3 comment contained a false SDK claim ("throwIfAborted() mirrors the SDK's internal abort"). Used the corrected comment per Phase 4 instructions: "Deliberately not the SDK's APIUserAbortError, and need not be."

## Test results

181 passed, 0 failed. All 19 Red tests now Green; 162 pre-existing tests unchanged.

Red→Green breakdown:
- `calculateBackoffDelay` (7): all pass
- `isRetryable` retryable cases (4): all pass; false cases (4): unchanged
- `defaultSleep` abort timing (2): both pass
- `TurnRunner — transient retry` (10): all pass including the key abort-reason identity test

## Live verification pending

This is an API-facing change. The retry loop works against the fakes; a real `overloaded_error` or `rate_limit_error` in production cannot be triggered on demand. Live verification is pending.

## Nothing else changed

`QueryRunner.ts` untouched. `main.ts` untouched (new constructor params are optional). No new exports on the public barrel.
