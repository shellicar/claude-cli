# PR #312 phases 1-2: Scout and Apostle

Scout found: `BetaToolResultBlockParam.content` already accepts `BetaRequestDocumentBlock`. No new delivery path needed.

Apostle built one anyway: magic `__toolAttachments` property, duck-typed runtime detection, parallel accumulation array, spreading alongside tool results in the user message. SC rejected it. Attachments go inside `tool_result.content`.

Full distillation in 2026-04-26.md.

