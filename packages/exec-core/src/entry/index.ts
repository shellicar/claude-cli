import { Executor } from '../Executor.js';
import { fromStream } from '../fromStream.js';
import type { CommandSpec, ExitStatus, IExecutor, PipelineOpts, PipelineStage, SpawnOpts } from '../types.js';

export type { CommandSpec, ExitStatus, IExecutor, PipelineOpts, PipelineStage, SpawnOpts };
export { Executor, fromStream };
