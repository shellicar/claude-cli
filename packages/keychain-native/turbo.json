{
  "$schema": "../../node_modules/turbo/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.rs", "**/*.js", "**/Cargo.*"],
      "outputs": ["target"],
      "outputLogs": "new-only"
    }
  }
}
